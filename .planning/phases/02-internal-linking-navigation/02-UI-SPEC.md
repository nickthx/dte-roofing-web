---
phase: 2
slug: internal-linking-navigation
status: draft
shadcn_initialized: false
preset: none
created: 2026-03-23
---

# Phase 2 — UI Design Contract

> Visual and interaction contract for the Internal Linking & Navigation phase. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none |
| Preset | not applicable |
| Component library | none (custom Tailwind components) |
| Icon library | lucide-react 0.344.0 |
| Font | System default (Tailwind sans stack: ui-sans-serif, system-ui, sans-serif) |

---

## Spacing Scale

Declared values (must be multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon-to-text gap in breadcrumb chevron spacing |
| sm | 8px | Gap between breadcrumb segments, inner padding of footer links |
| md | 16px | Container horizontal padding (px-4), card inner padding, footer gap-y |
| lg | 24px | Card padding (p-6), section heading bottom margin (mb-8) |
| xl | 32px | Grid gap between nearby-area cards (gap-4 = 16px horizontal, gap-8 = 32px for section margins) |
| 2xl | 48px | Not used in this phase |
| 3xl | 64px | Nearby areas section bottom margin (mb-16) |

Exceptions: Breadcrumb bar uses py-3 (12px vertical padding) -- this is the standard compact navigation bar height matching the site's existing nav density.

---

## Typography

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Body | 16px (text-base) | 400 (font-normal) | 1.5 |
| Label / Breadcrumb text | 14px (text-sm) | 500 (font-medium) | 1.43 |
| Card label | 14px (text-sm) | 400 (font-normal) | 1.43 |
| Section heading | 30px (text-3xl) | 700 (font-bold) | 1.2 |
| Card city name | 16px (text-base) | 700 (font-bold) | 1.5 |
| Footer column heading | 18px (text-lg) | 700 (font-bold) | 1.33 |
| Footer link | 14px (text-sm) | 400 (font-normal) | 1.43 |

Note: This phase introduces no new typography roles. All values match existing site patterns observed in Footer.tsx and location page content sections.

---

## Color

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | #ffffff (bg-white) | Page background, content sections |
| Secondary (30%) | #f9fafb (bg-gray-50) | Breadcrumb bar background, nearby-area cards, CTA sections |
| Accent (10%) | #b91c1c (primary-700) | Breadcrumb "All Service Areas" link text, MapPin icon fill, card hover border, "View Services" link text |
| Accent hover | #991b1b (primary-800) | Breadcrumb link hover state |
| Card hover surface | primary-50 (#fef2f2) | Nearby-area card background on hover |
| Border default | #e5e7eb (border-gray-200) | Breadcrumb bar bottom border, nearby-area card border |
| Border hover | #b91c1c (border-primary-700) | Nearby-area card border on hover |
| Text primary | #3d3d3d (charcoal-900) | Section headings, card city names |
| Text secondary | #4f4f4f (charcoal-700) | Current breadcrumb page name |
| Text muted | #9ca3af (text-gray-400) | Footer link text (default state), breadcrumb chevron icon |
| Footer surface | #3d3d3d (charcoal-900) | Footer background (existing, unchanged) |

Accent reserved for: breadcrumb hub link text, MapPin icons in nearby-area cards, card hover borders, "View Services" inline link text, "All Service Areas" footer link. Never applied to body text, section headings, or card backgrounds at rest.

---

## Component Inventory

### 1. LocationBreadcrumb

**File:** `src/components/LocationBreadcrumb.tsx`
**Props:** `{ cityName: string }`
**Placement:** Sibling element between hero `</section>` and main content `<section>` on all 13 location subpages.

| Property | Value |
|----------|-------|
| Container background | bg-gray-50 |
| Container border | border-b border-gray-200 |
| Container padding | py-3 (12px vertical) |
| Inner wrapper | container mx-auto px-4 |
| Nav element | `<nav aria-label="Breadcrumb">` |
| Layout | flex items-center gap-2 |
| Hub link text | "All Service Areas" |
| Hub link color | text-primary-700 hover:text-primary-800 font-medium |
| Separator icon | ChevronRight, w-4 h-4, text-gray-400 |
| Current page text | cityName prop, text-charcoal-700 font-medium |
| Transition | transition-colors on link hover |

**Interaction states:**
- Default: "All Service Areas" in primary-700, chevron in gray-400, city name in charcoal-700
- Hover on link: text shifts to primary-800
- Focus: browser default outline (do not suppress)

### 2. NearbyAreas

**File:** `src/components/NearbyAreas.tsx`
**Props:** `{ locationSlug: string }`
**Placement:** Inside the main content `<section>` of each location subpage, after existing content blocks and before the bottom CTA section. Wrapped in a `<div className="mb-16">`.

| Property | Value |
|----------|-------|
| Section heading | "Nearby Areas We Serve" |
| Heading style | text-3xl font-bold text-charcoal-900 mb-8 text-center |
| Grid layout | grid sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-5 gap-4 |
| Max neighbors shown | 5 (`.slice(0, 5)` on neighbors array) |
| Card element | `<Link>` to `/locations/{neighbor.slug}` |
| Card padding | p-6 |
| Card background | bg-gray-50 (default), hover:bg-primary-50 |
| Card border | border border-gray-200 (default), hover:border-primary-700 |
| Card radius | rounded-xl |
| Card text alignment | text-center |
| Card transition | transition-all on hover |
| Card group | `group` class for coordinated hover |
| MapPin icon | w-6 h-6 text-primary-700 mx-auto mb-3 |
| City name | font-bold text-charcoal-900, group-hover:text-primary-700 transition-colors |
| Sub-link text | "View Services", text-sm text-primary-700 mt-2 inline-flex items-center |
| ArrowRight icon | w-3 h-3 ml-1 (inline with "View Services") |

**Interaction states:**
- Default: gray-50 background, gray-200 border, charcoal-900 city name
- Hover: primary-50 background, primary-700 border, city name shifts to primary-700
- Focus: browser default outline (do not suppress)

**Data source:** `getLocationBySlug(locationSlug)` from `src/data/locations.ts`. Neighbors resolved via `LOCATIONS.find()`. Missing slugs filtered out silently.

**Empty state:** If `getLocationBySlug` returns null (invalid slug), component returns `null` -- no visible output. This is a defensive guard, not a user-facing state.

### 3. Footer Update (existing component modification)

**File:** `src/components/Footer.tsx`
**Change:** Replace hardcoded 5-city list in "Areas We Serve" column with dynamic list from `LOCATIONS` array.

| Property | Value |
|----------|-------|
| Column heading | "Areas We Serve" (unchanged) |
| Heading style | font-bold text-lg mb-4 (unchanged) |
| List layout | grid grid-cols-2 gap-x-4 gap-y-1.5 (NEW: 2-column sub-grid) |
| Link text size | text-sm (14px) |
| Link color | text-gray-400 hover:text-white transition-colors |
| "All Service Areas" link | Separate element below grid, text-sm font-medium, mt-3 inline-block |
| Arrow indicator | HTML entity `&rarr;` appended to "All Service Areas" |
| Data source | `LOCATIONS` array imported from `src/data/locations` |
| Sort order | Array order from locations.ts (alphabetical or as defined) |

**Rationale for 2-column sub-grid:** 13 cities + 1 "All Service Areas" link = 14 items. A single column would be ~2.5x taller than adjacent footer columns. Two columns of 7 items each keeps vertical height balanced. Reducing `space-y` from `space-y-2` (8px) to `gap-y-1.5` (6px) further compacts the list.

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Breadcrumb hub link | "All Service Areas" |
| Breadcrumb separator | ChevronRight icon (no text) |
| Breadcrumb current page | `{cityName}` (dynamic, e.g. "Columbus") |
| Nearby section heading | "Nearby Areas We Serve" |
| Nearby card sub-link | "View Services" |
| Footer column heading | "Areas We Serve" (unchanged) |
| Footer hub link | "All Service Areas &rarr;" |
| Primary CTA | Not applicable -- this phase adds navigation elements, not action CTAs |
| Empty state heading | Not applicable -- no empty states in this phase (all data is static from locations.ts) |
| Empty state body | Not applicable |
| Error state | Not applicable -- no async data fetching in new components |
| Destructive confirmation | Not applicable -- no destructive actions in this phase |

---

## Accessibility Contract

| Element | Requirement |
|---------|-------------|
| LocationBreadcrumb | `<nav aria-label="Breadcrumb">` wrapping element |
| Breadcrumb links | Standard `<Link>` with visible text, no aria-label override needed |
| NearbyAreas cards | Each card is a `<Link>` with city name as visible text -- sufficient for screen readers |
| NearbyAreas heading | `<h2>` for proper heading hierarchy (h1 is page title in hero) |
| Footer links | Standard `<Link>` elements with visible city name text |
| Keyboard navigation | All interactive elements are native `<Link>` -- tab order is automatic |
| Focus indicators | Browser default outlines preserved on all links (do not add outline-none) |
| Color contrast | text-gray-400 on charcoal-900 footer: contrast ratio 4.6:1 (passes AA for 14px text). text-primary-700 on white: contrast ratio 5.6:1 (passes AA). |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none | not applicable |
| Third-party | none | not applicable |

No shadcn or third-party registries are used in this project. All components are custom Tailwind + React.

---

## Interaction States Summary

| Component | Default | Hover | Focus | Active | Disabled |
|-----------|---------|-------|-------|--------|----------|
| Breadcrumb link | primary-700 text | primary-800 text | browser outline | n/a | n/a |
| Nearby card | gray-50 bg, gray-200 border | primary-50 bg, primary-700 border, primary-700 city text | browser outline | n/a | n/a |
| Footer city link | gray-400 text | white text | browser outline | n/a | n/a |
| Footer hub link | gray-400 text, font-medium | white text | browser outline | n/a | n/a |

---

## Responsive Behavior

| Breakpoint | NearbyAreas Grid | Footer Areas Layout |
|------------|------------------|---------------------|
| < 640px (mobile) | 1 column, full width cards | 2-column sub-grid (unchanged from desktop) |
| >= 640px (sm) | 2 columns | 2-column sub-grid |
| >= 768px (md) | 3 columns | 2-column sub-grid within md:grid-cols-5 footer |
| >= 1024px (lg) | 5 columns (1 per neighbor) | 2-column sub-grid within md:grid-cols-5 footer |

Breadcrumb is a single horizontal row at all breakpoints. No wrapping expected -- "All Service Areas > {City}" fits within mobile viewport at 14px.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
