---
phase: 03-core-pages-metadata
plan: 02
subsystem: seo
type: mechanical-application
autonomous: true
wave: 1
depends_on: [03-01]
requirements: [META-01, META-02, META-04, META-05]
---

# Phase 03 Plan 02: Core Pages Metadata Application

## Objective

Mechanically apply the 6 approved title/description pairs from `03-COPY-APPROVED.md` to the 6 core page source files, removing 2 META-05 superlative violations ("most trusted" in About.tsx:13, "highest-rated" in Reviews.tsx:75) in the process. Mirrors the Phase 02-02 atomic per-page commit pattern.

## Context

- @.planning/phases/03-core-pages-metadata/03-COPY-APPROVED.md — frozen approved copy (source of truth)
- @.planning/phases/02-service-pages-metadata/02-02-PLAN.md — pattern reference
- @.planning/phases/02-service-pages-metadata/02-02-SUMMARY.md — execution pattern (batched edit + single build + atomic commits)

## In-Scope Files (6)

| # | Route | File | Title line | Desc line | Special |
|---|-------|------|------------|-----------|---------|
| 1 | /about | `src/pages/About.tsx` | 12 | 13 | "most trusted" superlative removed |
| 2 | /gallery | `src/pages/Gallery.tsx` | 79 | 80 | SEO block nested inside JSX |
| 3 | /reviews | `src/pages/Reviews.tsx` | 74 | 75 | Template literal → plain string; "highest-rated" removed |
| 4 | /faq | `src/pages/FAQ.tsx` | 107 | 108 | Standard |
| 5 | /contact | `src/pages/Contact.tsx` | 14 | 15 | Standard; SchemaMarkup untouched |
| 6 | /get-a-quote-consultation | `src/pages/InstantQuote.tsx` | 11 | 12 | Standard; H1 untouched |

## Tasks

### Task 1: Update About.tsx metadata — type="auto"

Apply the approved title and description from `03-COPY-APPROVED.md` Section "1. About.tsx". This edit removes the "most trusted" superlative violation (META-05).

**Done when:** `src/pages/About.tsx` contains the approved strings verbatim; `grep -q "most trusted" src/pages/About.tsx` returns non-zero.

**Commit:** `feat(03-02): update about metadata`

### Task 2: Update Gallery.tsx metadata — type="auto"

Apply the approved strings from Section "2. Gallery.tsx". SEO block is nested inside JSX at lines 78-82.

**Commit:** `feat(03-02): update gallery metadata`

### Task 3: Update Reviews.tsx metadata — type="auto"

Apply the approved strings from Section "3. Reviews.tsx". Replace the entire template literal expression `{`...`}` with a plain double-quoted string. Do NOT delete `totalReviews` or `useReviewData` — referenced elsewhere. Removes "highest-rated" superlative (META-05).

**Commit:** `feat(03-02): update reviews metadata`

### Task 4: Update FAQ.tsx metadata — type="auto"

Apply strings from Section "4. FAQ.tsx".

**Commit:** `feat(03-02): update faq metadata`

### Task 5: Update Contact.tsx metadata — type="auto"

Apply strings from Section "5. Contact.tsx". NAP preserved: "615 Hilliard Rome Rd", "614-971-6028". SchemaMarkup (lines 19-24) NOT edited.

**Commit:** `feat(03-02): update contact metadata`

### Task 6: Update InstantQuote.tsx metadata — type="auto"

Apply strings from Section "6. InstantQuote.tsx". H1 (lines 24-26) NOT edited.

**Commit:** `feat(03-02): update instant-quote metadata`

### Task 7: Build + lint + typecheck gate — type="auto"

Run `npm run build` — must exit 0. Run `npm run lint` and `npm run typecheck` — pre-existing errors from Phase 02 deferred-items are expected and out of scope per SCOPE BOUNDARY. Only auto-fix issues caused by this plan's edits.

**Done when:** `npm run build` exits 0 and all 6 in-scope dist files exist.

### Task 8: Create scripts/verify-phase-03.sh — type="auto"

Mirror `scripts/verify-phase-02.sh`. Check each of the 6 in-scope dist files for: (a) expected helmet title, (b) no "BEST Roofer in Columbus", (c) helmet description tag present. Also assert zero "most trusted" and zero "highest-rated" in dist files.

**Commit:** `test(03-02): add phase 3 batch verification script`

### Task 9: Regenerate sitemap (side effect) — type="auto"

If `public/sitemap.xml` is modified by the build, commit separately.

**Commit:** `chore(03-02): regenerate sitemap`

## Execution Pattern

Use batched-edit + single-build optimization from Phase 02-02:
1. Edit all 6 source files first (parallel if possible)
2. Run `npm run build` ONCE
3. Run `bash scripts/verify-phase-03.sh`
4. Stage and commit each source file atomically (6 feat commits)
5. Commit verify script + sitemap side effect

## Success Criteria

- [ ] All 6 in-scope files contain the approved title/description strings verbatim
- [ ] Zero "BEST Roofer in Columbus" in the 6 in-scope source and dist files
- [ ] Zero "most trusted" in About.tsx / dist/about/index.html
- [ ] Zero "highest-rated" in Reviews.tsx / dist/reviews/index.html
- [ ] Reviews.tsx description is a plain double-quoted string, not a template literal
- [ ] `npm run build` exits 0
- [ ] `bash scripts/verify-phase-03.sh` exits 0
- [ ] 6 atomic per-page commits + 1 verify script commit

## Output

- `.planning/phases/03-core-pages-metadata/03-02-SUMMARY.md`
- `scripts/verify-phase-03.sh`
- 6 modified source files
