---
phase: 03-core-pages-metadata
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/03-core-pages-metadata/03-COPY-DRAFTS.md
  - .planning/phases/03-core-pages-metadata/03-COPY-APPROVED.md
autonomous: false
requirements:
  - META-01
  - META-02
  - META-04
  - META-05
tags:
  - seo
  - metadata
  - content

must_haves:
  truths:
    - "A single draft packet exists containing title + description text for all 6 in-scope core pages (/about, /gallery, /reviews, /faq, /contact, /get-a-quote-consultation)"
    - "Every drafted title is <=60 chars and describes the actual page content (not a duplicate boilerplate)"
    - "Every drafted description is 140-200 chars and contains no superlative language (best/top-rated/#1/premier/leading/finest/greatest/most trusted/highest-rated/award-winning)"
    - "All 6 titles are unique (zero duplicates) and all 6 descriptions are unique (zero duplicates)"
    - "User has explicitly approved the 6 drafted title+description pairs before any source files are edited"
    - "An approved copy packet exists at 03-COPY-APPROVED.md that Plan 03-02 can apply mechanically without decision-making"
  artifacts:
    - path: ".planning/phases/03-core-pages-metadata/03-COPY-DRAFTS.md"
      provides: "6 drafted title+description rows for user review"
      contains: "About DTE Roofing | Family-Owned Roofer in Columbus, OH"
    - path: ".planning/phases/03-core-pages-metadata/03-COPY-APPROVED.md"
      provides: "Frozen, user-approved copy packet for Plan 03-02 mechanical application"
      contains: "status: APPROVED"
  key_links:
    - from: ".planning/phases/03-core-pages-metadata/03-COPY-DRAFTS.md"
      to: "user approval checkpoint"
      via: "checkpoint:decision task"
      pattern: "checkpoint:decision"
    - from: ".planning/phases/03-core-pages-metadata/03-COPY-APPROVED.md"
      to: ".planning/phases/03-core-pages-metadata/03-02-PLAN.md"
      via: "Plan 03-02 depends_on: [03-01] reads 03-COPY-APPROVED.md"
      pattern: "03-COPY-APPROVED.md"
---

<objective>
Draft title + description text for all 6 in-scope core pages (/about, /gallery, /reviews, /faq, /contact, /get-a-quote-consultation) into a single reviewable packet, then halt for explicit user approval before any source file is touched. This plan ships ZERO source code edits. Its only outputs are two markdown files under the phase directory: a drafts document and an approved-and-frozen document.

Purpose: CLAUDE.md mandates "Complete each phase fully, verify, and get user approval before starting next phase". Phase 2 established the copy-approval-gate pattern (drafts -> checkpoint:decision -> frozen approved packet -> mechanical apply). This plan is the Phase 3 equivalent for the 6 core pages. It is intentionally `autonomous: false` because Plan 03-02 cannot begin until the user has reviewed every drafted title and description and signaled explicit approval.

Output: `03-COPY-DRAFTS.md` (drafter output) and `03-COPY-APPROVED.md` (frozen after user approval). Both live under `.planning/phases/03-core-pages-metadata/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-service-pages-metadata/02-01-SUMMARY.md
@.planning/phases/02-service-pages-metadata/02-02-SUMMARY.md
@.planning/phases/02-service-pages-metadata/02-COPY-APPROVED.md
@CLAUDE.md

<interfaces>
<!-- The SEO component's prop contract. This is a closed contract from Phase 2 — do NOT change the component, only change what is passed to it. Phase 3 scope is title + description ONLY (no H1/H2 rewrites — those are Phase 4 for locations). -->

From src/components/SEO.tsx:
```typescript
interface SEOProps {
  title: string;           // <- Plan 03-02 edits this
  description: string;     // <- Plan 03-02 edits this
  keywords?: string;       // NOT edited in Phase 3
  ogTitle?: string;        // NOT edited (defaults to title)
  ogDescription?: string;  // NOT edited (defaults to description)
  ogImage?: string;        // NOT edited
  canonical?: string;      // NOT edited
}
```

The prerender pipeline (`scripts/prerender.mjs`) behavior is identical to what Phase 2 verified against:
1. `npm run build` runs vite SSR -> scripts/prerender.mjs -> emits dist/<route>/index.html per route
2. react-helmet-async emits `<title data-rh="true">...</title>` and `<meta data-rh="true" name="description" ...>` tags into the head
3. Phase 2 Plan 02-02 applied a D-04b patch to scripts/prerender.mjs that ALSO strips the static template description, so dist files now contain exactly ONE description tag per page (the helmet one). Phase 3 verification greps should target `data-rh="true"` variant and NOT grep for the raw `<meta name="description"` pattern.

The 6 in-scope route slugs map to dist directories as:
- /about -> dist/about/index.html
- /gallery -> dist/gallery/index.html
- /reviews -> dist/reviews/index.html
- /faq -> dist/faq/index.html
- /contact -> dist/contact/index.html
- /get-a-quote-consultation -> dist/get-a-quote-consultation/index.html
</interfaces>

<constraints>
- **Content lock (CLAUDE.md + ROADMAP Phase 3 scope):** Only `<SEO title>` and `<SEO description>` props are in scope. Do NOT rewrite `<h1>`, body `<p>`, FAQ content, testimonials, or any other page content. H1 rewrites for core pages are OUT of scope for Phase 3 (only service pages got H1 rewrites in Phase 2; location pages will get H1 rewrites in Phase 4; core pages keep their brand-voice H1s).
- **No URL/slug changes:** App.tsx routes are NOT edited.
- **No NAP changes:** titles and descriptions may NOT alter business name, address (615 Hilliard Rome Rd), phone (614-971-6028), or email.
- **No new dependencies:** phase is pure string edits.
- **META-05 superlative ban:** drafted titles and descriptions MUST NOT contain `best`, `top-rated`, `#1`, `premier`, `leading`, `finest`, `greatest`, `award-winning`, `most trusted`, `highest-rated`, `unmatched`, `unbeatable`, or any synonyms. Note: the CURRENT About description contains "most trusted" and the CURRENT Reviews description contains "highest-rated" — both MUST be removed in the rewrite.
- **File-drafter rule (user global CLAUDE.md):** All working markdown files MUST live under `.planning/phases/03-core-pages-metadata/`, NEVER in repo root.
- **Pre-validated drafts (carry-through from Phase 2):** The 6 drafted strings below have already been character-budget verified (titles 50-57 chars <=60 cap; descriptions 165-178 chars within 140-200 budget) and superlative-audited (zero matches). The drafter's job is to materialize them into the drafts file, not to redraft them.
</constraints>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Draft copy packet for all 6 in-scope core pages</name>
  <files>.planning/phases/03-core-pages-metadata/03-COPY-DRAFTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (META-01, META-02, META-04, META-05 exact wording)
    - .planning/ROADMAP.md (Phase 3 section — success criteria anchor)
    - .planning/phases/02-service-pages-metadata/02-COPY-APPROVED.md (reference format for approved copy packet structure)
    - .planning/phases/02-service-pages-metadata/02-01-SUMMARY.md (reference execution pattern: drafts -> checkpoint -> freeze)
    - src/pages/About.tsx (lines 1-30 — confirm current title line 12, description line 13, H1 line 22 match drafts table)
    - src/pages/Gallery.tsx (lines 75-95 — confirm SEO block is at lines 78-82, title line 79, description line 80, H1 line 89)
    - src/pages/Reviews.tsx (lines 70-90 — confirm SEO block is at lines 73-77, title line 74, description line 75 is a TEMPLATE LITERAL `{`Read ${totalReviews}...`}`, H1 line 84)
    - src/pages/FAQ.tsx (lines 100-130 — confirm SEO block title line 107, description line 108, H1 line 123)
    - src/pages/Contact.tsx (lines 10-35 — confirm SEO block title line 14, description line 15, H1 line 29)
    - src/pages/InstantQuote.tsx (lines 5-30 — confirm SEO block title line 11, description line 12, H1 line 24 is multi-line spanning lines 24-26)
    - CLAUDE.md (content lock + user approval gate constraints)
  </read_first>
  <action>
Create the file `.planning/phases/03-core-pages-metadata/03-COPY-DRAFTS.md` containing EXACTLY the content below. Do NOT invent alternative phrasings, synonyms, or "improved" versions — the drafts below have already been character-budget verified (titles <=60, descriptions 140-200) and superlative-audited via node. Your job is to materialize them into the drafts file, not to redraft them.

The drafts file MUST contain the following sections in order:

# Phase 3 - Core Pages Copy Drafts

**Status:** DRAFT - awaiting user approval
**Scope:** 6 core pages: /about, /gallery, /reviews, /faq, /contact, /get-a-quote-consultation
**Validator:** All titles verified <=60 chars, all descriptions verified 140-200 chars via `node -e 't.length'`
**Superlative audit:** Zero matches against `best|top-rated|#1|premier|leading|finest|greatest|award-winning|most trusted|highest-rated`

## Scope (ROADMAP Phase 3 Anchor)

Phase 3 rewrites ONLY the `<SEO title>` and `<SEO description>` props on 6 core pages. This phase does NOT touch H1, H2, body copy, testimonials, FAQs, or any other page content. Per ROADMAP Phase 3:

> Goal: 6 non-location/non-service pages have unique, accurate titles and descriptions matching their actual content
> Success Criteria:
>   1. /about, /gallery, /reviews, /faq, /contact, /get-a-quote-consultation all have unique titles
>   2. All 6 have unique 140-200 char descriptions
>   3. No "BEST" superlatives; descriptions match page intent
>   4. Build passes; prerendered HTML contains new metadata

H1 rewrites for core pages are out of scope (they retain their brand-voice H1s like "Two Brothers, One Mission", "Let's Talk About Your Roof", etc.).

## In-Scope Files (6)

Line numbers verified against current source via grep.

| # | Route | File | SEO block line | Title line | Description line | H1 line | Notes |
|---|-------|------|----------------|------------|-------------------|---------|-------|
| 1 | /about | `src/pages/About.tsx` | 11 | 12 | 13 | 22 | Description currently contains "most trusted" — superlative per META-05 |
| 2 | /gallery | `src/pages/Gallery.tsx` | 78 | 79 | 80 | 89 | SEO block is nested inside lightbox JSX, NOT at the top of the component |
| 3 | /reviews | `src/pages/Reviews.tsx` | 73 | 74 | 75 | 84 | Description is a TEMPLATE LITERAL `{`Read ${totalReviews} verified five-star reviews ... highest-rated ...`}` — must be replaced with a plain string literal and "highest-rated" superlative removed |
| 4 | /faq | `src/pages/FAQ.tsx` | 106 | 107 | 108 | 123 | Standard SEO block structure |
| 5 | /contact | `src/pages/Contact.tsx` | 13 | 14 | 15 | 29 | Standard structure; Contact.tsx also has a SchemaMarkup component at lines 19-24 which is OUT of scope |
| 6 | /get-a-quote-consultation | `src/pages/InstantQuote.tsx` | 10 | 11 | 12 | 24 (spans 24-26) | Route is `/get-a-quote-consultation` (not `/instant-quote`) - verified in App.tsx:92 |

**IMPORTANT - Reviews.tsx description is a template literal, not a plain string.** The current code is:

```tsx
description={`Read ${totalReviews} verified five-star reviews from Columbus homeowners. See why DTE Roofing is the highest-rated roofing company in Columbus, OH.`}
```

Plan 03-02 MUST replace this entire expression (including the braces and backticks) with a plain double-quoted string literal. The `totalReviews` variable is a runtime value from `useReviewData()` and does not appear in prerendered HTML correctly anyway (it was always a static interpolation at render time). The drafted description below is a plain string with no interpolation.

**IMPORTANT - Gallery.tsx SEO block is nested.** The SEO component appears at line 78 inside a later JSX fragment (after the lightbox modal), not at the top of the component return. Plan 03-02 must use the exact string from line 79 (title) and line 80 (description) as the Edit tool's `old_string`.

## Drafted Copy - 6 Pages

Each row below contains the exact strings Plan 03-02 will write. Character counts are pre-verified via node.

### 1. About.tsx (`/about`)

| Field | Value | Chars |
|-------|-------|-------|
| title | `About DTE Roofing \| Family-Owned Roofer in Columbus, OH` | 55 |
| description | `Learn about DTE Roofing, a family-owned roofing company serving Columbus and Central Ohio since 2023. Licensed, insured, and committed to honest, quality workmanship.` | 166 |

Current strings that will be replaced:
- L12 title: `BEST Roofer in Columbus - if you're looking for Honest Roofing Services near me or Expert Roof Repair & Replacement near me - DTE Roofing is the place to be.` (duplicate boilerplate, 166 chars)
- L13 description: `Learn about DTE Roofing, Columbus's most trusted roofing company. Quality workmanship, honest service, and dedication to excellence.` (contains "most trusted" superlative)

H1 "Two Brothers, One Mission: Roofing Done Right" (line 22) is NOT edited.

### 2. Gallery.tsx (`/gallery`)

| Field | Value | Chars |
|-------|-------|-------|
| title | `Roofing Project Gallery \| DTE Roofing Columbus, OH` | 50 |
| description | `Browse our portfolio of completed roof replacements, repairs, and commercial roofing projects across Columbus, Dublin, Hilliard, and Central Ohio. See the DTE Roofing difference.` | 178 |

Current strings (lines 79, 80 - nested SEO block inside JSX):
- L79 title: duplicate boilerplate
- L80 description: `View our portfolio of completed roofing projects in Columbus, Dublin, and Hilliard OH. See quality roof installations, repairs, and commercial roofing work by DTE Roofing.` (unique but contains "quality" which is fine, just being refreshed)

H1 "See the Work That Speaks for Itself" (line 89) is NOT edited.

### 3. Reviews.tsx (`/reviews`)

| Field | Value | Chars |
|-------|-------|-------|
| title | `DTE Roofing Reviews \| Central Ohio Homeowners Speak Out` | 55 |
| description | `Read verified reviews from DTE Roofing customers across Columbus and Central Ohio. Real feedback on roof repairs, replacements, and installations from local homeowners.` | 168 |

Current strings:
- L74 title: duplicate boilerplate
- L75 description: template literal ``{`Read ${totalReviews} verified five-star reviews from Columbus homeowners. See why DTE Roofing is the highest-rated roofing company in Columbus, OH.`}`` - contains "highest-rated" superlative AND is a template literal (must become a plain string)

H1 "Don't Take Our Word for It - Hear From Our Customers" (line 84) is NOT edited.

**Note on variable cleanup:** After replacing the description with a plain string, the `totalReviews` variable may become unused depending on where else it is referenced. Plan 03-02 must NOT delete `totalReviews` or `useReviewData` usage — it is likely referenced elsewhere in the page body (e.g., in the H2 or review count display). Plan 03-02's only concern is the description prop on line 75.

### 4. FAQ.tsx (`/faq`)

| Field | Value | Chars |
|-------|-------|-------|
| title | `Roofing FAQs \| Questions Answered by DTE Roofing Columbus` | 57 |
| description | `Answers to common roofing questions about costs, timelines, warranties, insurance claims, and materials. Expert guidance from DTE Roofing in Columbus, OH. Call 614-971-6028.` | 173 |

Current strings:
- L107 title: duplicate boilerplate
- L108 description: `Get answers to common roofing questions. Learn about costs, timelines, warranties, insurance claims, and more. Expert guidance from Columbus's trusted roofing contractor.` (unique but being refreshed for consistency)

H1 "Your Roofing Questions, Answered Honestly" (line 123) is NOT edited.

### 5. Contact.tsx (`/contact`)

| Field | Value | Chars |
|-------|-------|-------|
| title | `Contact DTE Roofing \| Free Estimates in Columbus, OH` | 52 |
| description | `Contact DTE Roofing for a free roofing estimate in Columbus and Central Ohio. Located at 615 Hilliard Rome Rd. Call 614-971-6028 or email for 24/7 emergency service.` | 165 |

Current strings:
- L14 title: duplicate boilerplate
- L15 description: `Contact DTE Roofing for free estimates on roof repair, replacement & installation. Located at 615 Hilliard Rome Rd, Columbus OH. Call 614-971-6028. Fast response, 24/7 emergency service.` (unique but being refreshed)

H1 "Let's Talk About Your Roof" (line 29) is NOT edited. The SchemaMarkup component (lines 19-24) is NOT edited.

**NAP verification:** address "615 Hilliard Rome Rd" and phone "614-971-6028" are preserved verbatim in the new description (NAP lock).

### 6. InstantQuote.tsx (`/get-a-quote-consultation`)

| Field | Value | Chars |
|-------|-------|-------|
| title | `Get an Instant Roof Quote \| DTE Roofing Columbus, OH` | 52 |
| description | `Get an instant roof replacement quote in under 60 seconds using satellite imagery. Accurate estimates with no inspection needed to get started. Serving Central Ohio.` | 165 |

Current strings:
- L11 title: duplicate boilerplate
- L12 description: `Get an accurate roof replacement quote in 30 seconds. Satellite imagery estimates with no inspection needed to get started.` (short, being expanded to include region coverage)

H1 "Get Your Roof Quote in Under 60 Seconds" (lines 24-26, multi-line JSX) is NOT edited.

## Character Budget Validation

All 6 drafts were validated pre-write via node:

```
about                      title= 55 [OK] | desc=166 [OK]
gallery                    title= 50 [OK] | desc=178 [OK]
reviews                    title= 55 [OK] | desc=168 [OK]
faq                        title= 57 [OK] | desc=173 [OK]
contact                    title= 52 [OK] | desc=165 [OK]
get-a-quote-consultation   title= 52 [OK] | desc=165 [OK]
```

All titles 50-57 chars (<=60 SERP cap). All descriptions 165-178 chars (within 140-200 budget).

## Superlative Audit (META-05)

Grep against the drafted titles + descriptions for `\b(best|top[- ]?rated|#1|premier|leading|finest|greatest|award[- ]?winning|most trusted|highest[- ]?rated|unmatched|unbeatable)\b` returns ZERO matches. All drafts comply with META-05.

The current source contains 2 superlative violations that the new drafts remove:
- `About.tsx:13` contains "most trusted" — removed in new draft
- `Reviews.tsx:75` contains "highest-rated" — removed in new draft

## Uniqueness Check (META-01, META-02)

All 6 drafted titles are distinct. All 6 drafted descriptions are distinct. Zero duplicates across the 6 core pages.

## Plain-Text Copy Roster (for verify-grep compatibility)

GFM tables escape pipes as `\|`, which breaks literal grep. The plain-text block below lists the 6 titles and descriptions with unescaped pipes so verification greps can match the literal SERP strings.

```
[about]
title: About DTE Roofing | Family-Owned Roofer in Columbus, OH
description: Learn about DTE Roofing, a family-owned roofing company serving Columbus and Central Ohio since 2023. Licensed, insured, and committed to honest, quality workmanship.

[gallery]
title: Roofing Project Gallery | DTE Roofing Columbus, OH
description: Browse our portfolio of completed roof replacements, repairs, and commercial roofing projects across Columbus, Dublin, Hilliard, and Central Ohio. See the DTE Roofing difference.

[reviews]
title: DTE Roofing Reviews | Central Ohio Homeowners Speak Out
description: Read verified reviews from DTE Roofing customers across Columbus and Central Ohio. Real feedback on roof repairs, replacements, and installations from local homeowners.

[faq]
title: Roofing FAQs | Questions Answered by DTE Roofing Columbus
description: Answers to common roofing questions about costs, timelines, warranties, insurance claims, and materials. Expert guidance from DTE Roofing in Columbus, OH. Call 614-971-6028.

[contact]
title: Contact DTE Roofing | Free Estimates in Columbus, OH
description: Contact DTE Roofing for a free roofing estimate in Columbus and Central Ohio. Located at 615 Hilliard Rome Rd. Call 614-971-6028 or email for 24/7 emergency service.

[get-a-quote-consultation]
title: Get an Instant Roof Quote | DTE Roofing Columbus, OH
description: Get an instant roof replacement quote in under 60 seconds using satellite imagery. Accurate estimates with no inspection needed to get started. Serving Central Ohio.
```

## What Plan 03-02 Will Do After Approval

1. Read `03-COPY-APPROVED.md` for the final strings
2. Apply each drafted title and description to the corresponding source file as 6 atomic per-page commits (one commit per source file, commit message pattern `feat(phase-03): update <slug> metadata`)
3. For Reviews.tsx: replace the template literal description expression with a plain double-quoted string
4. Run `npm run build && npm run lint && npx tsc --noEmit` (all must exit 0)
5. Grep each `dist/<slug>/index.html` for the new `data-rh="true"` title and description strings
6. Confirm no dist file contains "BEST Roofer in Columbus" (legacy duplicate boilerplate removed)
7. Create `scripts/verify-phase-03.sh` as a batch verification script mirroring `scripts/verify-phase-02.sh`
  </action>
  <verify>
    <automated>test -f .planning/phases/03-core-pages-metadata/03-COPY-DRAFTS.md && grep -q "About DTE Roofing | Family-Owned Roofer in Columbus, OH" .planning/phases/03-core-pages-metadata/03-COPY-DRAFTS.md && grep -q "Roofing Project Gallery | DTE Roofing Columbus, OH" .planning/phases/03-core-pages-metadata/03-COPY-DRAFTS.md && grep -q "DTE Roofing Reviews | Central Ohio Homeowners Speak Out" .planning/phases/03-core-pages-metadata/03-COPY-DRAFTS.md && grep -q "Roofing FAQs | Questions Answered by DTE Roofing Columbus" .planning/phases/03-core-pages-metadata/03-COPY-DRAFTS.md && grep -q "Contact DTE Roofing | Free Estimates in Columbus, OH" .planning/phases/03-core-pages-metadata/03-COPY-DRAFTS.md && grep -q "Get an Instant Roof Quote | DTE Roofing Columbus, OH" .planning/phases/03-core-pages-metadata/03-COPY-DRAFTS.md && grep -q "Learn about DTE Roofing, a family-owned roofing company serving Columbus and Central Ohio since 2023" .planning/phases/03-core-pages-metadata/03-COPY-DRAFTS.md && grep -q "Plain-Text Copy Roster" .planning/phases/03-core-pages-metadata/03-COPY-DRAFTS.md && [ -z "$(git status --porcelain src/ 2>/dev/null)" ]</automated>
  </verify>
  <acceptance_criteria>
- [ ] File `.planning/phases/03-core-pages-metadata/03-COPY-DRAFTS.md` exists
- [ ] File contains all 6 exact title strings verifiable by `grep -q "EXACT_TITLE"` (see `<verify>` block — 6 title greps)
- [ ] File contains all 6 exact description strings (at minimum the first 80+ chars of each verifiable via `grep -q`)
- [ ] File contains a `## Plain-Text Copy Roster` section with unescaped pipes (bypass GFM table pipe-escape false-negative from Phase 2 lesson learned)
- [ ] Each of the 6 drafted titles has length <=60 chars, validated by: `node -e 'const t="About DTE Roofing | Family-Owned Roofer in Columbus, OH"; if(t.length>60)process.exit(1)'` (run for all 6)
- [ ] Each of the 6 drafted descriptions has length between 140 and 200 chars, validated by: `node -e 'const d="..."; if(d.length<140||d.length>200)process.exit(1)'` (run for all 6)
- [ ] Zero superlatives in drafted strings: `! grep -iE '\b(best|top-rated|#1|premier|leading|finest|greatest|award-winning|most trusted|highest-rated)\b' .planning/phases/03-core-pages-metadata/03-COPY-DRAFTS.md | grep -v "Current strings" | grep -v "BEST Roofer in Columbus" | grep -v "most trusted" | grep -v "highest-rated"` (the file references the OLD strings in "Current strings that will be replaced" sections — those references are allowed; the new drafts themselves contain no superlatives)
- [ ] No source files under `src/` have been modified: `git diff --stat src/` returns empty
  </acceptance_criteria>
  <done>03-COPY-DRAFTS.md exists with all 6 pages drafted, character counts validated, superlative audit clean, and zero src/ modifications.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: User approval gate — approve drafted copy before any source edits</name>
  <decision>
Before Plan 03-02 edits any source files, the user must explicitly approve the 6 drafted title+description pairs documented in `03-COPY-DRAFTS.md`. The user may:
1. Approve all 6 pairs as drafted (unblocks Plan 03-02 immediately)
2. Approve with specific modifications (requires executor to update drafts, re-validate, re-present)
3. Reject and request a redraft
  </decision>
  <context>
Phase 2 established the copy-approval gate pattern for content-sensitive edits: draft -> checkpoint:decision -> freeze -> mechanical apply. CLAUDE.md mandates user approval before phase execution.

The drafts file presents all 6 pages with exact copy so the user can review every string before Plan 03-02 runs. All drafts are pre-validated for character budget (titles <=60, descriptions 140-200) and superlative compliance (META-05). If the user rejects any draft, the executor must update `03-COPY-DRAFTS.md`, re-validate character budgets and superlative audit, and re-present. No source files are edited until the user approves.
  </context>
  <options>
    <option id="approve-all">
      <name>Approve all 6 drafted title+description pairs as written</name>
      <pros>
        - Unblocks Plan 03-02 immediately
        - All drafts pre-validated against character budgets and superlative ban
        - Removes 2 existing superlative violations ("most trusted" in About, "highest-rated" in Reviews)
        - Consistent voice and structure with Phase 2 approved copy
      </pros>
      <cons>
        - None identified (drafts are conservative refreshes, not aggressive rewrites)
      </cons>
    </option>
    <option id="approve-with-mods">
      <name>Approve with specific modifications (user edits one or more strings)</name>
      <pros>
        - User retains full control over final voice
      </pros>
      <cons>
        - Executor must update 03-COPY-DRAFTS.md, re-run character budget validation, re-run superlative audit, re-present
        - May require multiple approval rounds
      </cons>
    </option>
    <option id="reject-redraft">
      <name>Reject and request a redraft with different direction</name>
      <pros>
        - Full reset if user wants a fundamentally different angle
      </pros>
      <cons>
        - Full reset cost; drafter re-runs Task 1 with new direction
      </cons>
    </option>
  </options>
  <read_first>
    - .planning/phases/03-core-pages-metadata/03-COPY-DRAFTS.md (created by Task 1)
  </read_first>
  <acceptance_criteria>
- [ ] User has provided explicit written approval for all 6 drafted pairs (or explicit modifications for any pair they want changed)
- [ ] Executor has NOT modified any file under `src/`: `git status --porcelain src/` returns empty
- [ ] Executor has NOT run `npm run build` yet
  </acceptance_criteria>
  <resume-signal>User responds with "approved", "approve all", or a list of specific modifications per slug (e.g., "about: change title to X")</resume-signal>
  <files>(none — this task only blocks on user input and modifies no files)</files>
  <action>This is a blocking checkpoint task. The executor MUST halt execution here and present the contents of `03-COPY-DRAFTS.md` to the user along with a clear approve/modify/reject prompt. The executor MUST NOT proceed to Task 3 until the user has provided explicit written approval (or explicit modifications to apply). No files are created, modified, or deleted in this task - its sole output is the user's approval captured for use in Task 3.</action>
  <verify>
    <automated>[ -z "$(git status --porcelain src/ scripts/ 2>/dev/null)" ] && test -f .planning/phases/03-core-pages-metadata/03-COPY-DRAFTS.md</automated>
  </verify>
  <done>User has explicitly approved all 6 drafted title+description pairs (with any modifications applied back to 03-COPY-DRAFTS.md and re-validated). No source files modified. Executor is ready to run Task 3.</done>
</task>

<task type="auto">
  <name>Task 3: Freeze approved copy packet</name>
  <files>.planning/phases/03-core-pages-metadata/03-COPY-APPROVED.md</files>
  <read_first>
    - .planning/phases/03-core-pages-metadata/03-COPY-DRAFTS.md
    - User's approval response from Task 2 checkpoint
    - .planning/phases/02-service-pages-metadata/02-COPY-APPROVED.md (reference format for YAML frontmatter + Plan Execution Instructions section)
  </read_first>
  <action>
Create `.planning/phases/03-core-pages-metadata/03-COPY-APPROVED.md` by copying `03-COPY-DRAFTS.md` and:

1. Adding a YAML frontmatter block at the top of the file. The file written to disk MUST begin with `---` on line 1, contain the fields below, and end with a closing `---` line before the rest of the document content:

```
status: APPROVED
approved_at: <ISO timestamp>
approved_by: user
source: 03-COPY-DRAFTS.md
scope: 6 core pages (about, gallery, reviews, faq, contact, get-a-quote-consultation)
approval_path: <"approve-all" | "approve-with-mods" | "other-per-user-response">
modifications: <list any user-requested modifications here, or "none">
```

2. Changing the document title from `# Phase 3 - Core Pages Copy Drafts` to `# Phase 3 - Core Pages Copy APPROVED` and updating the Status line from "DRAFT - awaiting user approval" to "APPROVED - frozen for Plan 03-02 mechanical application".

3. If the user approved with modifications, apply each modification VERBATIM to the corresponding row in the "Drafted Copy - 6 Pages" table AND the "Plain-Text Copy Roster" section. Re-run the node character-budget validator against any modified strings to confirm they still fit the 140-200 / <=60 budget. If a modified string fails validation, STOP and return to the user for another revision.

4. Adding a new section at the END of the file titled `## Plan 03-02 Execution Instructions` containing:

```
Plan 03-02 MUST read this file and apply the exact title and description strings to each source file. Any deviation from these strings is a task failure.

Mechanical application rules:
- About.tsx line 12 title: Edit tool with exact old_string (current 166-char BEST title) -> new_string (approved title)
- About.tsx line 13 description: Edit tool with exact old_string (current "most trusted" description) -> new_string (approved description)
- Gallery.tsx lines 79 and 80: Edit tool; note the SEO block is nested inside JSX at lines 78-82, not at the top of the component
- Reviews.tsx line 74 title: standard Edit
- Reviews.tsx line 75 description: TEMPLATE LITERAL REPLACEMENT. The current expression is `description={\`Read ${totalReviews} verified five-star reviews from Columbus homeowners. See why DTE Roofing is the highest-rated roofing company in Columbus, OH.\`}`. Plan 03-02 must replace the ENTIRE expression including `{\`...\`}` braces with a plain double-quoted string: `description="Read verified reviews ..."`. The `totalReviews` variable is NOT deleted — it remains referenced elsewhere in the Reviews.tsx body (review count display). Only the description prop changes form.
- FAQ.tsx lines 107 and 108: standard Edit
- Contact.tsx lines 14 and 15: standard Edit. Do NOT touch SchemaMarkup (lines 19-24).
- InstantQuote.tsx lines 11 and 12: standard Edit. Do NOT touch H1 (lines 24-26).

Commit pattern (atomic per page):
- `feat(phase-03): update about metadata`
- `feat(phase-03): update gallery metadata`
- `feat(phase-03): update reviews metadata`
- `feat(phase-03): update faq metadata`
- `feat(phase-03): update contact metadata`
- `feat(phase-03): update instant-quote metadata` (filename slug is instant-quote; route is /get-a-quote-consultation)

Final verification task:
- Create scripts/verify-phase-03.sh mirroring scripts/verify-phase-02.sh
- Run `npm run build && npm run lint && npx tsc --noEmit` — all exit 0
- Run `bash scripts/verify-phase-03.sh` — exit 0
- Assert `grep -c '<title data-rh="true">' dist/{about,gallery,reviews,faq,contact,get-a-quote-consultation}/index.html` returns 1 per page
- Assert zero "BEST Roofer in Columbus" matches across all 6 dist files
```

5. Do NOT modify any file under `src/`. This task only creates a frozen markdown artifact.
  </action>
  <verify>
    <automated>test -f .planning/phases/03-core-pages-metadata/03-COPY-APPROVED.md && grep -q "status: APPROVED" .planning/phases/03-core-pages-metadata/03-COPY-APPROVED.md && grep -q "Plan 03-02 Execution Instructions" .planning/phases/03-core-pages-metadata/03-COPY-APPROVED.md && grep -q "About DTE Roofing | Family-Owned Roofer in Columbus, OH" .planning/phases/03-core-pages-metadata/03-COPY-APPROVED.md && grep -q "Roofing Project Gallery | DTE Roofing Columbus, OH" .planning/phases/03-core-pages-metadata/03-COPY-APPROVED.md && grep -q "DTE Roofing Reviews | Central Ohio Homeowners Speak Out" .planning/phases/03-core-pages-metadata/03-COPY-APPROVED.md && grep -q "Roofing FAQs | Questions Answered by DTE Roofing Columbus" .planning/phases/03-core-pages-metadata/03-COPY-APPROVED.md && grep -q "Contact DTE Roofing | Free Estimates in Columbus, OH" .planning/phases/03-core-pages-metadata/03-COPY-APPROVED.md && grep -q "Get an Instant Roof Quote | DTE Roofing Columbus, OH" .planning/phases/03-core-pages-metadata/03-COPY-APPROVED.md && [ -z "$(git status --porcelain src/ 2>/dev/null)" ]</automated>
  </verify>
  <acceptance_criteria>
- [ ] File `.planning/phases/03-core-pages-metadata/03-COPY-APPROVED.md` exists
- [ ] File starts with YAML frontmatter containing `status: APPROVED`, `approved_at`, `approved_by`, `scope`, `approval_path`
- [ ] File contains all 6 approved titles verbatim (6 grep checks in `<verify>` block)
- [ ] File contains `## Plan 03-02 Execution Instructions` section
- [ ] Execution Instructions section explicitly calls out the Reviews.tsx template literal replacement requirement
- [ ] Execution Instructions section explicitly calls out the InstantQuote.tsx slug-vs-route mismatch (filename = instant-quote, route = /get-a-quote-consultation)
- [ ] Each modified string (if any) re-validates against 140-200 / <=60 budget via node
- [ ] No source files modified: `git diff --stat src/ scripts/` returns empty
  </acceptance_criteria>
  <done>Approved, frozen copy packet exists. Plan 03-02 has unambiguous input. Phase 3 Plan 01 is complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

N/A — Plan 03-01 is a pure planning-artifact plan. It creates two markdown files under `.planning/phases/03-core-pages-metadata/`. No source code, no runtime code, no network calls, no user input handling, no authentication, no data persistence. Zero trust boundaries introduced.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | Spoofing | N/A | N/A | No identity surface touched. Plan only writes markdown files under `.planning/`. |
| T-03-01-02 | Tampering | 03-COPY-DRAFTS.md / 03-COPY-APPROVED.md | accept | Files land via atomic git commits. Any future modification is visible in `git log .planning/phases/03-core-pages-metadata/`. Branch protection / review policies are handled at the repo level, not the plan level. |
| T-03-01-03 | Repudiation | git authorship | accept | Commits are signed per repository git config. Plan does not bypass signing or use `--no-verify`. |
| T-03-01-04 | Information Disclosure | Drafted copy | mitigate | Drafted title/description strings reference only public data: business name "DTE Roofing", public phone 614-971-6028, public address 615 Hilliard Rome Rd, public founding year 2023, public service area (Columbus + Central Ohio). No employee names, internal paths, unpublished phone numbers, internal URLs, staging hostnames, or API keys appear in drafts. Verified by reading all 6 drafted strings. |
| T-03-01-05 | Denial of Service | N/A | N/A | No runtime surface. Markdown files are read-only to the build. |
| T-03-01-06 | Elevation of Privilege | N/A | N/A | No auth surface, no role changes, no capability grants. |

**Confirmation:**
- No secrets or PII introduced: only public NAP data present in drafted strings.
- No new trust boundaries: plan produces 2 markdown files under .planning/, nothing else.
- No runtime code changes: Plan 03-01 is `autonomous: false` and only writes markdown.
- Atomic commit boundary: `03-COPY-DRAFTS.md` and `03-COPY-APPROVED.md` land as 2 separate commits (not bundled with source edits), making surgical revert trivial if content is ever found defective.
</threat_model>

<verification>
Phase 3 Plan 01 is complete when ALL of the following are true:

1. `.planning/phases/03-core-pages-metadata/03-COPY-DRAFTS.md` exists with 6 drafted title+description pairs
2. All 6 drafted titles pass character budget check (<=60 chars)
3. All 6 drafted descriptions pass character budget check (140-200 chars)
4. Zero superlative matches across all 6 drafted strings
5. All 6 titles are unique, all 6 descriptions are unique
6. Plan 01 Task 2 checkpoint has received explicit user approval (or approval with modifications, with modifications re-validated)
7. `.planning/phases/03-core-pages-metadata/03-COPY-APPROVED.md` exists with `status: APPROVED` YAML frontmatter
8. 03-COPY-APPROVED.md contains `## Plan 03-02 Execution Instructions` section with mechanical application rules
9. `git diff --stat src/ scripts/` returns empty (Plan 01 is source-edit-free)
10. 2 atomic commits exist: one for Task 1 (drafts), one for Task 3 (approved)
</verification>

<success_criteria>
- 03-COPY-DRAFTS.md: 6 title+description pairs, character-budget verified, superlative-clean, uniqueness-verified
- User approval gate resolved
- 03-COPY-APPROVED.md: frozen copy packet with YAML frontmatter + execution instructions
- Zero source edits in Plan 01 (source edits are Plan 02's scope)
- Plan 03-02 can run mechanically with unambiguous input
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-pages-metadata/03-01-SUMMARY.md` using the standard summary template. Include:
- Performance metrics (duration, task commits)
- Decisions captured (user approval path and any modifications)
- Self-check confirming both artifacts exist and no src/ files modified
- Next phase readiness note unblocking Plan 03-02
</output>
