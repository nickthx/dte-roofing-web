---
phase: 04-location-descriptions-h1-h2
plan: 02
subsystem: seo
type: mechanical-application
autonomous: true
wave: 2
depends_on: [04-01]
requirements: [META-02, META-04, META-05, HEAD-01, HEAD-03]
files_modified:
  - src/pages/locations/Columbus.tsx
  - src/pages/locations/Delaware.tsx
  - src/pages/locations/Dublin.tsx
  - src/pages/locations/Gahanna.tsx
  - src/pages/locations/GroveCity.tsx
  - src/pages/locations/Hilliard.tsx
  - src/pages/locations/NewAlbany.tsx
  - src/pages/locations/Pickerington.tsx
  - src/pages/locations/Powell.tsx
  - src/pages/locations/Reynoldsburg.tsx
  - src/pages/locations/UpperArlington.tsx
  - src/pages/locations/Westerville.tsx
  - src/pages/locations/Worthington.tsx
  - scripts/verify-phase-04.sh
must_haves:
  truths:
    - "All 13 location source files contain the approved description, H1, and H2 verbatim"
    - "npm run build exits 0 and prerendered dist/locations/*/index.html contain the new metadata"
    - "13 atomic per-page commits exist in git history"
    - "Zero META-05 superlatives in modified source files"
  artifacts:
    - path: "scripts/verify-phase-04.sh"
      provides: "Batch verification of the 13 prerendered location pages"
    - path: "src/pages/locations/*.tsx"
      provides: "13 updated location page source files"
  key_links:
    - from: "src/pages/locations/*.tsx"
      to: "dist/locations/*/index.html"
      via: "npm run build (prerender pipeline)"
      pattern: "helmet title/desc in prerendered HTML"
---

# Phase 04 Plan 02: Location Metadata Mechanical Application

## Objective

Mechanically apply the 13 approved description/H1/H2 sets from `04-COPY-APPROVED.md` to the 13 location source files, one atomic commit per page. Run a single build at the end, execute a batch verification script against prerendered HTML, and gate on build + lint + typecheck success. Mirrors Phase 02-02 / 03-02.

## Context

- @.planning/phases/04-location-descriptions-h1-h2/04-COPY-APPROVED.md — frozen source of truth
- @.planning/phases/03-core-pages-metadata/03-02-PLAN.md — pattern reference (batched edit + single build + atomic commits)
- @.planning/phases/02-service-pages-metadata/02-02-SUMMARY.md — execution pattern outcome
- @scripts/verify-phase-03.sh — template for verification script
- @src/pages/locations/Columbus.tsx — representative page structure

## Scope Boundary

- ONLY edit: SEO description prop, hero `<h1>` text, primary services section `<h2>` text — at the exact line numbers recorded in `04-COPY-APPROVED.md`
- DO NOT edit: URLs, SchemaMarkup props (separate concern), NAP, paragraph content, neighborhood lists, testimonials, review counts, canonical URLs, SEO title (unless approved packet explicitly says otherwise)
- DO NOT refactor unrelated code or fix pre-existing lint warnings

## In-Scope Files (13)

All 13 files under `src/pages/locations/`. Exact edit targets come from the audit table in `04-COPY-APPROVED.md` — do not re-derive line numbers; use the frozen packet.

## Tasks

### Task 1: Load approved packet and verify freshness — type="auto"

Read `04-COPY-APPROVED.md`. Confirm `frozen: true` in frontmatter. Confirm 13 entries. If the file is missing or not frozen, STOP and report the blocker — do not proceed.

**Done when:** Packet loaded, frozen flag verified, 13 entries confirmed present.

### Task 2: Apply Columbus.tsx — type="auto"

Edit `src/pages/locations/Columbus.tsx`:
- Replace SEO `description` prop at the line number from the approved packet
- Replace hero `<h1>` text content
- Replace primary services `<h2>` text content

Use string-exact Edit tool replacements. Do not touch surrounding JSX, SchemaMarkup, or any other content.

**Verify:** `grep` confirms the approved description, H1, and H2 strings exist in the file; grep for "BEST Roofer", "most trusted", "highest-rated", "#1" returns non-zero (absent).

**Commit:** `feat(04-02): update columbus metadata`

### Task 3: Apply Delaware.tsx — type="auto"

Same pattern as Task 2 against `src/pages/locations/Delaware.tsx` using the Delaware entry from the approved packet.

**Commit:** `feat(04-02): update delaware metadata`

### Task 4: Apply Dublin.tsx — type="auto"

**Commit:** `feat(04-02): update dublin metadata`

### Task 5: Apply Gahanna.tsx — type="auto"

**Commit:** `feat(04-02): update gahanna metadata`

### Task 6: Apply GroveCity.tsx — type="auto"

**Commit:** `feat(04-02): update grove-city metadata`

### Task 7: Apply Hilliard.tsx — type="auto"

**Commit:** `feat(04-02): update hilliard metadata`

### Task 8: Apply NewAlbany.tsx — type="auto"

**Commit:** `feat(04-02): update new-albany metadata`

### Task 9: Apply Pickerington.tsx — type="auto"

**Commit:** `feat(04-02): update pickerington metadata`

### Task 10: Apply Powell.tsx — type="auto"

**Commit:** `feat(04-02): update powell metadata`

### Task 11: Apply Reynoldsburg.tsx — type="auto"

**Commit:** `feat(04-02): update reynoldsburg metadata`

### Task 12: Apply UpperArlington.tsx — type="auto"

**Commit:** `feat(04-02): update upper-arlington metadata`

### Task 13: Apply Westerville.tsx — type="auto"

**Commit:** `feat(04-02): update westerville metadata`

### Task 14: Apply Worthington.tsx — type="auto"

**Commit:** `feat(04-02): update worthington metadata`

### Task 15: Build + lint + typecheck gate — type="auto"

Run once, after all 13 edits are staged/committed:

```bash
npm run build     # MUST exit 0
npm run lint      # pre-existing warnings deferred; no NEW errors from these edits
npm run typecheck # pre-existing warnings deferred; no NEW errors from these edits
```

**Done when:** `npm run build` exits 0 and all 13 `dist/locations/*/index.html` files exist.

### Task 16: Create scripts/verify-phase-04.sh — type="auto"

Mirror `scripts/verify-phase-03.sh`. For each of the 13 prerendered files (`dist/locations/columbus/index.html`, etc.) assert:
1. Helmet `<title>` tag present (title was preserved from v1.0 unless packet said otherwise)
2. Helmet `<meta name="description">` contains the approved description string (or a distinctive substring from it)
3. Rendered `<h1>` contains the approved primary-service + city phrasing
4. Rendered primary services `<h2>` contains the approved secondary-services phrasing
5. Zero matches for "BEST Roofer", "most trusted", "highest-rated", "#1" across all 13 dist files
6. All 13 descriptions are unique (no duplicates across dist files)

Script exits 0 only if all 13 pages pass all 6 checks.

**Commit:** `test(04-02): add phase 4 batch verification script`

### Task 17: Run verification script — type="auto"

Run `bash scripts/verify-phase-04.sh`. Must exit 0.

**Done when:** Script reports 13/13 pages passing all checks.

### Task 18: Regenerate sitemap if modified — type="auto"

If `public/sitemap.xml` or `dist/sitemap.xml` changed as a side effect of the build, commit separately.

**Commit (if applicable):** `chore(04-02): regenerate sitemap`

## Execution Pattern

Mirror Phase 02-02 / 03-02 optimization:
1. Edit all 13 source files first (batch via parallel Edit calls where possible)
2. Run `npm run build` ONCE after all edits
3. Create and run `scripts/verify-phase-04.sh`
4. Stage and commit each source file atomically (13 feat commits) — can be done as a batched commit sequence if git staging allows per-file isolation
5. Commit verify script + sitemap side effect

## Success Criteria

- [ ] All 13 `src/pages/locations/*.tsx` files contain the approved description, H1, and H2 strings verbatim
- [ ] Zero "BEST Roofer", "most trusted", "highest-rated", "#1" in the 13 source files and 13 dist files
- [ ] `npm run build` exits 0
- [ ] All 13 `dist/locations/*/index.html` files exist and contain approved metadata
- [ ] `bash scripts/verify-phase-04.sh` exits 0
- [ ] 13 atomic `feat(04-02): update {city} metadata` commits + 1 test commit (+ optional sitemap chore commit)
- [ ] 13 unique descriptions, 13 unique H1s, 13 unique H2s confirmed in dist output
- [ ] No URL, NAP, SchemaMarkup, or paragraph content changes

## Output

- 13 modified `src/pages/locations/*.tsx` files
- `scripts/verify-phase-04.sh`
- `.planning/phases/04-location-descriptions-h1-h2/04-02-SUMMARY.md`
