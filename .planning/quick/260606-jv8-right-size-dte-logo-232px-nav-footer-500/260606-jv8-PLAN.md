---
quick_id: 260606-jv8
description: Right-size DTE logo (232px nav/footer + 500px -large for Home founders) and verify content images serve WebP
date: 2026-06-06
mode: quick (inline — gsd-* subagent types unavailable in this harness)
---

# Quick Task 260606-jv8 — Plan

## Background

Audit asked for (a) content images in /images served as WebP with srcset + JPG fallback, and (b) `DTE-Roofing-Logo-two-Men.png` resized from 500x277 to ~232x128 (2x of nav's h-16 display).

Investigation findings:
- (a) is already shipped: zero raw `<img>` tags in src/ — Gallery, WorkCarousel, About, Footer, Navigation, Home hero all render via `<Picture>` (`<source type="image/webp" srcset>` + raster fallback), and every content JPG has a .webp sibling (most also .avif) in public/images. Remaining .jpg/.png refs are og:image / JSON-LD metadata, intentionally raster. Only build verification needed.
- (b) has a conflict: `Home.tsx:104` renders the same logo file at `w-full` in a md:grid-cols-2 column (~600px on desktop). User decision: **two variants** — shrink base file to 232x128 for nav/footer/schema; add `-large` (500x277) variant used only by the Home founders section.

## Tasks

### Task 1 — Logo variants

**Files:** `public/images/DTE-Roofing-Logo-two-Men{,-large}.{png,webp}`, `src/pages/Home.tsx`, `scripts/optimize-images.mjs`

**Action:**
1. Copy current 500x277 png/webp to `DTE-Roofing-Logo-two-Men-large.{png,webp}` (re-encode via sharp, lossless webp, palette png).
2. Resize base `DTE-Roofing-Logo-two-Men.{png,webp}` to 232px wide (fit inside → 232x128).
3. Point `Home.tsx` founders `<Picture>` at the `-large` variant (width/height stay 500/277).
4. Update the logo jobs in `scripts/optimize-images.mjs` so re-runs are idempotent: base regenerated at maxWidth 232 from the `-large` source; `-large` job added at maxWidth 500.

**Verify:** sharp metadata reports 232x128 for base and 500x277 for -large; Nav/Footer/SchemaMarkup still reference base name; Home references -large.

**Done:** Nav/footer ship ~2.6x fewer logo pixels; Home founders section unchanged visually; schema logo (232x128) still ≥ Google's 112px minimum.

### Task 2 — Build + verify WebP serving

**Files:** none (verification)

**Action:** `npm run build` (verification gate). Confirm in dist output: `/about/index.html` and `/gallery/index.html` contain `<source type="image/webp"` for content images; homepage logo picture references the 232px base; founders section references `-large`.

**Verify:** grep counts > 0 for webp sources on content pages; no references to deleted paths.

**Done:** Markup verified to serve WebP with raster fallback site-wide.

## must_haves

- Base logo file is 232x128; -large is 500x277; Home founders section uses -large; nav/footer/schema use base
- Build succeeds; content pages emit `<source type="image/webp">` per image
- No URL/NAP/content changes; no new dependencies (sharp already a devDependency)
