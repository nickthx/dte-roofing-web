---
phase: quick
plan: x50
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/logos/BbbLogo.tsx
  - src/components/logos/NextdoorLogo.tsx
  - src/components/Footer.tsx
  - src/pages/Home.tsx
  - src/pages/About.tsx
autonomous: true
must_haves:
  truths:
    - "BBB placeholder divs replaced with recognizable BBB torch logo in all 3 files"
    - "Nextdoor placeholder divs replaced with recognizable Nextdoor logo in all 3 files"
    - "Logos render at correct sizes (w-6 h-6 in Footer, w-10 h-10 in Home/About)"
  artifacts:
    - path: "src/components/logos/BbbLogo.tsx"
      provides: "Reusable BBB inline SVG component"
    - path: "src/components/logos/NextdoorLogo.tsx"
      provides: "Reusable Nextdoor inline SVG component"
  key_links:
    - from: "Footer.tsx, Home.tsx, About.tsx"
      to: "BbbLogo, NextdoorLogo components"
      via: "import and render"
---

<objective>
Replace BBB and Nextdoor placeholder colored divs with proper inline SVG logo components across Footer, Home, and About pages.

Purpose: Eliminate placeholder "BBB" and "ND" text blocks with professional-looking logo representations.
Output: Two reusable SVG logo components, three files updated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/components/Footer.tsx (lines 116-133 — placeholder badges)
@src/pages/Home.tsx (lines 492-510 — accreditation badges)
@src/pages/About.tsx (lines 148-159 — accreditation badges)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create BBB and Nextdoor inline SVG logo components</name>
  <files>src/components/logos/BbbLogo.tsx, src/components/logos/NextdoorLogo.tsx</files>
  <action>
Create `src/components/logos/` directory with two components:

**BbbLogo.tsx:**
- Accept `className` prop (string, optional) for sizing
- Render an inline SVG of the BBB Accredited Business torch logo
- The BBB logo is a blue torch/flame design. Use a simplified SVG representation: blue (#004B8D) background rounded rectangle with a white torch/flame shape in the center. The torch has a handle at the bottom and flame at top.
- Default className should be empty string so parent controls sizing
- Export as default function component

**NextdoorLogo.tsx:**
- Accept `className` prop (string, optional) for sizing
- Render an inline SVG of the Nextdoor logo
- The Nextdoor logo is a green (#00B636) stylized house shape (roofline forming an inverted V / chevron with a small square cutout representing a door/window). Use a simplified SVG: green background rounded rectangle with white house silhouette.
- Default className should be empty string so parent controls sizing
- Export as default function component

Both components: use `viewBox="0 0 40 40"` so they scale to any size via className. Apply `aria-hidden="true"` since adjacent text provides context.
  </action>
  <verify>
    <automated>npx tsc --noEmit src/components/logos/BbbLogo.tsx src/components/logos/NextdoorLogo.tsx 2>&1 | head -20</automated>
  </verify>
  <done>Both logo components exist, export default functions, accept className prop, render inline SVGs, pass TypeScript check</done>
</task>

<task type="auto">
  <name>Task 2: Replace all placeholder divs with logo components</name>
  <files>src/components/Footer.tsx, src/pages/Home.tsx, src/pages/About.tsx</files>
  <action>
In each file, add imports for BbbLogo and NextdoorLogo from `../components/logos/BbbLogo` and `../components/logos/NextdoorLogo` (adjust relative path per file location).

**Footer.tsx (lines ~117-118, ~126-127):**
- Replace the BBB placeholder div (`w-6 h-6 bg-blue-700 rounded flex items-center justify-center flex-shrink-0 mt-1` containing "BBB" span) with: `<BbbLogo className="w-6 h-6 flex-shrink-0 mt-1" />`
- Replace the Nextdoor placeholder div (`w-6 h-6 bg-green-600 rounded flex items-center justify-center flex-shrink-0 mt-1` containing "ND" span) with: `<NextdoorLogo className="w-6 h-6 flex-shrink-0 mt-1" />`

**Home.tsx (lines ~494-495, ~503-504):**
- Replace the BBB placeholder div (`w-10 h-10 bg-blue-700 rounded-lg flex items-center justify-center flex-shrink-0` containing "BBB" span) with: `<BbbLogo className="w-10 h-10 flex-shrink-0" />`
- Replace the Nextdoor placeholder div (`w-10 h-10 bg-green-600 rounded-lg flex items-center justify-center flex-shrink-0` containing "ND" span) with: `<NextdoorLogo className="w-10 h-10 flex-shrink-0" />`

**About.tsx (lines ~150-151, ~159-160):**
- Same replacements as Home.tsx with `w-10 h-10 flex-shrink-0` className.

Keep all surrounding markup (text labels, container divs) unchanged.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>All 6 placeholder divs (2 per file x 3 files) replaced with logo components. Build succeeds with no errors. No placeholder "BBB" or "ND" text spans remain in these badge sections.</done>
</task>

</tasks>

<verification>
- `npm run build` completes without errors
- grep for placeholder patterns confirms removal: `grep -rn "font-extrabold text-xs.*BBB\|font-extrabold text-xs.*ND" src/components/Footer.tsx src/pages/Home.tsx src/pages/About.tsx` returns no matches
- Logo components are imported and used in all 3 files
</verification>

<success_criteria>
- Zero placeholder colored divs with "BBB" or "ND" text remain in badge sections
- BBB and Nextdoor logos render as recognizable inline SVGs at appropriate sizes
- Build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/quick/260325-x50-replace-bbb-and-nextdoor-placeholder-log/260325-x50-SUMMARY.md`
</output>
