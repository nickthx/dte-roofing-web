---
phase: quick-260411-m0s
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - vercel.json
autonomous: true
requirements:
  - SEC-HEADERS-B2-01
  - SEC-HEADERS-B2-02
must_haves:
  truths:
    - "Responses on all routes include a Permissions-Policy header with 8 disallowed APIs (camera, microphone, geolocation, payment, usb, gyroscope, accelerometer, magnetometer)"
    - "Responses on all routes include an enforcing Content-Security-Policy header (not Report-Only)"
    - "The CSP directive value string is byte-identical to the previous Content-Security-Policy-Report-Only value"
    - "Content-Security-Policy-Report-Only no longer appears in vercel.json (replaced, not duplicated)"
    - "All pre-existing headers (X-Frame-Options, X-Content-Type-Options, Referrer-Policy, Strict-Transport-Security) remain intact and unchanged"
    - "All pre-existing redirects, rewrites, cleanUrls, and trailingSlash settings remain untouched"
    - "vercel.json remains valid JSON that parses without error"
  artifacts:
    - path: "vercel.json"
      provides: "Vercel routing + headers config with Permissions-Policy added and CSP enforcing"
      contains: "Permissions-Policy"
  key_links:
    - from: "vercel.json /(.*) headers array"
      to: "all HTTP responses from dteroofingllc.com"
      via: "Vercel edge header injection"
      pattern: "Permissions-Policy.*camera=\\(\\)"
    - from: "vercel.json /(.*) headers array"
      to: "browser CSP enforcement"
      via: "Content-Security-Policy header (enforcing)"
      pattern: "\"key\": \"Content-Security-Policy\""
---

<objective>
Apply security headers batch 2 to vercel.json in two atomic edits: (1) add a restrictive Permissions-Policy header disallowing 8 browser APIs this marketing site does not use, and (2) promote the existing Report-Only CSP to an enforcing CSP by renaming only the key (value byte-identical).

Purpose: Close remaining medium-severity security header gaps surfaced in the 2026-04-11 audit. The Report-Only CSP has been live without complaints and the user has explicitly authorized promotion despite the absence of a report-uri log to review.
Output: Updated vercel.json with 6 security headers on /(.*) (X-Frame-Options, X-Content-Type-Options, Referrer-Policy, Strict-Transport-Security, Permissions-Policy, Content-Security-Policy).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@vercel.json

<interfaces>
<!-- Current vercel.json /(.*) headers array state (post commit 542bdda / 1ee0b31) -->
<!-- Executor should use these exact values — no exploration needed -->

From vercel.json (lines 18-29):
```json
{
  "source": "/(.*)",
  "headers": [
    { "key": "X-Frame-Options", "value": "DENY" },
    { "key": "X-Content-Type-Options", "value": "nosniff" },
    { "key": "Referrer-Policy", "value": "strict-origin-when-cross-origin" },
    { "key": "Strict-Transport-Security", "value": "max-age=63072000; includeSubDomains; preload" },
    { "key": "Content-Security-Policy-Report-Only", "value": "default-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval' https://app.roofle.com https://static.cloudflareinsights.com; style-src 'self' 'unsafe-inline' https://fonts.googleapis.com; font-src 'self' https://fonts.gstatic.com data:; img-src 'self' data: https: blob:; connect-src 'self' https://ujasdbelviyamnwxjgth.supabase.co https://app.roofle.com https://cloudflareinsights.com https://*.cloudflareinsights.com; frame-src https://app.roofle.com; object-src 'none'; base-uri 'self'; form-action 'self'" }
  ]
}
```

Target final state of the headers array after both tasks complete:
```json
{
  "source": "/(.*)",
  "headers": [
    { "key": "X-Frame-Options", "value": "DENY" },
    { "key": "X-Content-Type-Options", "value": "nosniff" },
    { "key": "Referrer-Policy", "value": "strict-origin-when-cross-origin" },
    { "key": "Strict-Transport-Security", "value": "max-age=63072000; includeSubDomains; preload" },
    { "key": "Permissions-Policy", "value": "camera=(), microphone=(), geolocation=(), payment=(), usb=(), gyroscope=(), accelerometer=(), magnetometer=()" },
    { "key": "Content-Security-Policy", "value": "default-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval' https://app.roofle.com https://static.cloudflareinsights.com; style-src 'self' 'unsafe-inline' https://fonts.googleapis.com; font-src 'self' https://fonts.gstatic.com data:; img-src 'self' data: https: blob:; connect-src 'self' https://ujasdbelviyamnwxjgth.supabase.co https://app.roofle.com https://cloudflareinsights.com https://*.cloudflareinsights.com; frame-src https://app.roofle.com; object-src 'none'; base-uri 'self'; form-action 'self'" }
  ]
}
```

MUST NOT TOUCH anywhere else in vercel.json:
- The 9 entries in `redirects` (apex→www, legacy path 301s, gutter/maintenance renames)
- `cleanUrls: true`
- `trailingSlash: false`
- `rewrites` array (/blog/:slug → /index.html)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Permissions-Policy header to /(.*) headers array</name>
  <files>vercel.json</files>
  <action>
Use the Edit tool to insert ONE new header entry into the `/(.*)` headers array in vercel.json, placed immediately AFTER the Strict-Transport-Security entry and BEFORE the Content-Security-Policy-Report-Only entry. This groups it with the other defensive security headers.

New entry to insert (exact JSON, preserve 2-space indentation matching surrounding lines):
```
        { "key": "Permissions-Policy", "value": "camera=(), microphone=(), geolocation=(), payment=(), usb=(), gyroscope=(), accelerometer=(), magnetometer=()" },
```

Note the trailing comma — the line AFTER it (the CSP-Report-Only entry) must remain the final entry in the array in this task, so the new Permissions-Policy line gets a trailing comma and the CSP entry stays as-is (still Report-Only, still no trailing comma since Task 2 handles that rename).

Rationale for the 8 disallowed APIs: this is a static marketing site with a lead form and a third-party roof-quote widget (Roofle). None of camera, microphone, geolocation, payment, usb, gyroscope, accelerometer, or magnetometer are used by any page, component, or the Roofle widget. Denying them with an empty allowlist prevents any future third-party script (including compromised ones) from requesting these APIs.

DO NOT touch:
- The redirects array (9 entries)
- X-Frame-Options, X-Content-Type-Options, Referrer-Policy, Strict-Transport-Security entries
- The Content-Security-Policy-Report-Only entry (Task 2 handles it)
- cleanUrls, trailingSlash, rewrites
  </action>
  <verify>
    <automated>node -e "const j=JSON.parse(require('fs').readFileSync('vercel.json','utf8'));const h=j.headers[0].headers;const pp=h.find(x=>x.key==='Permissions-Policy');if(!pp)throw new Error('Permissions-Policy missing');if(pp.value!=='camera=(), microphone=(), geolocation=(), payment=(), usb=(), gyroscope=(), accelerometer=(), magnetometer=()')throw new Error('Permissions-Policy value mismatch: '+pp.value);if(!h.find(x=>x.key==='Strict-Transport-Security'))throw new Error('HSTS missing');if(!h.find(x=>x.key==='Content-Security-Policy-Report-Only'))throw new Error('CSP Report-Only must still exist at this point (Task 2 handles rename)');if(h.length!==6)throw new Error('Expected 6 headers, got '+h.length);console.log('OK: Permissions-Policy added, 6 headers total, CSP still Report-Only')"</automated>
  </verify>
  <done>
vercel.json parses as valid JSON. The `/(.*)` headers array contains exactly 6 entries in order: X-Frame-Options, X-Content-Type-Options, Referrer-Policy, Strict-Transport-Security, Permissions-Policy, Content-Security-Policy-Report-Only. The Permissions-Policy value is exactly `camera=(), microphone=(), geolocation=(), payment=(), usb=(), gyroscope=(), accelerometer=(), magnetometer=()`. No other vercel.json content changed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Promote CSP Report-Only to enforcing (rename key only)</name>
  <files>vercel.json</files>
  <action>
Use the Edit tool to change ONLY the key string in the CSP header entry from `Content-Security-Policy-Report-Only` to `Content-Security-Policy`. The value string MUST remain byte-identical — this is purely a key rename, nothing else.

Target line change in vercel.json (only the key name — not the value, not whitespace, not anything else):
- Before: `{ "key": "Content-Security-Policy-Report-Only", "value": "default-src 'self'; ..." }`
- After:  `{ "key": "Content-Security-Policy", "value": "default-src 'self'; ..." }`

CRITICAL warnings:
1. Do NOT retype the value string. Use Edit's old_string/new_string to replace ONLY `"Content-Security-Policy-Report-Only"` with `"Content-Security-Policy"`. Any accidental modification to the value breaks the policy.
2. The CSP is now ENFORCING. Any violation (missing source in an allowlist, unexpected inline script hash, new third-party) will be hard-blocked by the browser. The user has explicitly authorized this despite the absence of report-uri logs to pre-review.
3. Do NOT add a report-uri in this task — explicitly out of scope.
4. Do NOT touch Permissions-Policy (added in Task 1) or any other header.
5. Do NOT touch redirects, rewrites, cleanUrls, or trailingSlash.

Context for safety: the CSP value allows 'self' + 'unsafe-inline' + 'unsafe-eval' for scripts, Roofle (app.roofle.com), Cloudflare insights, Supabase (ujasdbelviyamnwxjgth.supabase.co), and Google Fonts. All currently-used assets and third-parties are covered, so the enforcement switch should be functionally invisible to end users.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const raw=fs.readFileSync('vercel.json','utf8');const j=JSON.parse(raw);const h=j.headers[0].headers;const csp=h.find(x=>x.key==='Content-Security-Policy');if(!csp)throw new Error('Enforcing CSP missing');if(h.find(x=>x.key==='Content-Security-Policy-Report-Only'))throw new Error('Report-Only CSP must be gone, not duplicated');const expected=\"default-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval' https://app.roofle.com https://static.cloudflareinsights.com; style-src 'self' 'unsafe-inline' https://fonts.googleapis.com; font-src 'self' https://fonts.gstatic.com data:; img-src 'self' data: https: blob:; connect-src 'self' https://ujasdbelviyamnwxjgth.supabase.co https://app.roofle.com https://cloudflareinsights.com https://*.cloudflareinsights.com; frame-src https://app.roofle.com; object-src 'none'; base-uri 'self'; form-action 'self'\";if(csp.value!==expected)throw new Error('CSP value drifted from pre-rename baseline:\\nGOT: '+csp.value+'\\nEXP: '+expected);const keys=h.map(x=>x.key);const want=['X-Frame-Options','X-Content-Type-Options','Referrer-Policy','Strict-Transport-Security','Permissions-Policy','Content-Security-Policy'];if(JSON.stringify(keys)!==JSON.stringify(want))throw new Error('Header key set/order wrong: '+JSON.stringify(keys));if(j.redirects.length!==9)throw new Error('Redirects count changed: '+j.redirects.length);if(j.cleanUrls!==true)throw new Error('cleanUrls changed');if(j.trailingSlash!==false)throw new Error('trailingSlash changed');if(!j.rewrites||j.rewrites.length!==1)throw new Error('rewrites changed');console.log('OK: CSP enforcing, value byte-identical, all 6 headers correct, redirects/rewrites untouched')" &amp;&amp; npm run build</automated>
  </verify>
  <done>
vercel.json parses as valid JSON. The `/(.*)` headers array contains exactly these 6 keys in order: X-Frame-Options, X-Content-Type-Options, Referrer-Policy, Strict-Transport-Security, Permissions-Policy, Content-Security-Policy. Content-Security-Policy-Report-Only no longer appears anywhere in the file. The enforcing CSP value is byte-identical to the previous Report-Only value. Redirects (9 entries), rewrites (1 entry), cleanUrls (true), trailingSlash (false) all unchanged. `npm run build` succeeds.
  </done>
</task>

</tasks>

<verification>
End-state assertions (covered by Task 2's automated check):
1. `node -e "JSON.parse(require('fs').readFileSync('vercel.json','utf8'))"` succeeds
2. The `/(.*)` headers array has exactly 6 entries in the specified order
3. Permissions-Policy value equals the 8-API deny-list string exactly
4. Content-Security-Policy key exists and Content-Security-Policy-Report-Only does not
5. CSP value is byte-identical to the pre-rename Report-Only value
6. Redirects (9), rewrites (1), cleanUrls (true), trailingSlash (false) all unchanged
7. `npm run build` succeeds (sanity check — Vite does not read vercel.json, but confirms nothing else broke)

Post-deploy (out of scope for this plan, user will verify after push):
- `curl -sI https://www.dteroofingllc.com/` shows both Permissions-Policy and Content-Security-Policy headers
- Browser DevTools Console shows no new CSP violation reports on home, a service page, and a location page after enforcement flip
</verification>

<success_criteria>
- [ ] Task 1 committed: Permissions-Policy added between HSTS and CSP-Report-Only
- [ ] Task 2 committed: CSP key renamed from Report-Only to enforcing, value byte-identical
- [ ] Both commits leave vercel.json as valid JSON
- [ ] `npm run build` succeeds after Task 2
- [ ] No modifications to redirects, rewrites, cleanUrls, trailingSlash, or any of the 4 pre-existing security headers
- [ ] No new dependencies, no content/component/styling changes anywhere in the repo
</success_criteria>

<output>
After completion, create `.planning/quick/260411-m0s-security-headers-batch-2-add-permissions/260411-m0s-SUMMARY.md`
</output>
