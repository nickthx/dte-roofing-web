---
quick_id: 260615-g1y
type: execute
wave: 1
depends_on: []
autonomous: true
files_modified:
  - scripts/generate-responsive-variants.mjs
  - src/data/imageVariants.ts
  - package.json
  - src/components/Picture.tsx
  - src/components/SEO.tsx
  - src/pages/Home.tsx
  - src/components/WorkCarousel.tsx
  - src/pages/Gallery.tsx
  - src/pages/About.tsx
  - public/images/   # generated -<w> variants only; never overwrites existing bases
requirements: [QUICK-260615-g1y]

must_haves:
  truths:
    - "The hero LCP image and large on-page photos serve a width-appropriate variant on mobile instead of one fixed-resolution file."
    - "The hero head preload fetches the SAME variant the <picture> <source> picks (no double-download, no LCP regression)."
    - "No srcSet descriptor ever names a width larger than the source image's intrinsic width (no upscaling, no srcSet lies)."
    - "Untouched call sites (headshots, logos, nav/footer) render byte-identically to before."
    - "npm run build still prerenders all 41 routes clean."
  artifacts:
    - path: "scripts/generate-responsive-variants.mjs"
      provides: "sharp variant generator + manifest writer (network-free, idempotent)"
      min_lines: 60
    - path: "src/data/imageVariants.ts"
      provides: "Generated typed manifest of real emitted widths per base image"
      contains: "export const imageVariants"
    - path: "src/components/Picture.tsx"
      provides: "Responsive <source srcSet/sizes> when sizes prop + manifest entry present; unchanged otherwise"
      contains: "sizes"
    - path: "src/components/SEO.tsx"
      provides: "Responsive hero preload via imageSrcSet/imageSizes"
      contains: "imageSrcSet"
  key_links:
    - from: "src/components/Picture.tsx"
      to: "src/data/imageVariants.ts"
      via: "import imageVariants, look up by src"
      pattern: "imageVariants\\["
    - from: "src/components/SEO.tsx"
      to: "src/data/imageVariants.ts"
      via: "import imageVariants for preload srcset"
      pattern: "imageVariants"
    - from: "src/pages/Home.tsx"
      to: "src/components/SEO.tsx"
      via: "preloadImageSizes prop"
      pattern: "preloadImageSizes"
---

<objective>
Add responsive `srcset`/`sizes` to the LCP hero and the large on-page photographic
images (homepage WorkCarousel, Gallery grid, Home/About project grids) so mobile
visitors download a width-appropriate variant instead of one fixed-resolution file.

Out of scope (confirmed with user): the small headshots
(`donovan-davis-dte-roofing`, `mitchell-davis-dte-roofing`, `andrew-watts-dte-roofing`)
and all logos (`DTE-Roofing-Logo-two-Men*`). The "Areas We Serve" grid on Home is
text Links, not images — also out of scope.

Purpose: Cut mobile image bytes (medium-ROI perf item deferred from the 2026-06-15
audit) without touching approved content, URLs, slugs, or NAP.

Output:
- A new network-free sharp script that emits `slug-<w>.<fmt>` variants and a typed
  manifest of each image's REAL emitted widths.
- A backward-compatible `Picture` that emits responsive `<source>`s only when given
  a `sizes` prop AND a manifest entry; identical output otherwise.
- A responsive hero head preload (`imageSrcSet`/`imageSizes`) that matches the
  `<source>` the browser actually picks.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/STATE.md

# The <picture> component to extend (single resolution today):
@src/components/Picture.tsx
# The hero preload emitter (lines ~65-73 must become responsive):
@src/components/SEO.tsx
# In-scope call sites:
@src/pages/Home.tsx
@src/components/WorkCarousel.tsx
@src/pages/Gallery.tsx
# SSOT for project image slugs (carousel + gallery + About):
@src/data/projects.ts
# REFERENCE ONLY — sharp conventions, quality, the resolveInput skip-on-rerun
# pattern, and the "cap LONGEST side with fit:inside" detail. DO NOT run it.
@scripts/optimize-images.mjs

<verified_facts>
1. Hero `hero-roofing-professional.{webp,jpg}` is **1200x630** (NOT 1920). Variant
   ceiling = 1200. Hero uses `avif={false}` — webp + jpg only.
2. Project photos have VARYING intrinsic widths — confirmed by reading the files:
   - `columbus-residential-roof-replacement.jpg` → 1599x902 (landscape, width 1599)
   - `pickerington-shingle-and-deck-replacement.jpg`, `dte-roofing-completed-project-central-ohio.jpg`,
     `gahanna-premium-designer-shingles.jpg`, `dublin-premium-asphalt-shingles.jpg`,
     `powell-architectural-shingle-design.jpg`, `dte-roofing-shingle-installation-columbus.jpg`
     → 900x1600 (portrait, RENDERED WIDTH 900)
   - `dte-roofing-job-site-columbus.jpg` → 500x833 (width 500 — from the ladder
     only a 400 variant is valid; 800/1200/1600 would all upscale)
   A fixed `[400,800,1200,1600]` ladder applied blindly would stamp "1200w" on a
   900px file — a srcSet lie. THEREFORE the manifest must record each image's REAL
   emitted width, and `Picture` must build `srcSet` only from real widths.
3. CRITICAL — "width descriptor" = the image's RENDERED WIDTH, which for a portrait
   photo is the SHORTER side. `optimize-images.mjs:55` caps the LONGEST side with
   `fit:'inside', withoutEnlargement:true`. So for a 900x1600 portrait, asking sharp
   for `width:800` yields an 800x1422 file whose width is 800 — good. But asking for
   `width:1200` yields 900x1600 (clamped, no enlargement) whose width is STILL 900 —
   that would be a duplicate of the 900-wide base, NOT a 1200-wide image. The script
   MUST read back `info.width` from `toFile()` and use THAT as the descriptor,
   de-duplicating any ladder rungs that clamp to the same actual width. Never trust
   the requested ladder value as the descriptor.
4. Hero IS preloaded: `Home.tsx:27` → `preloadImage="/images/hero-roofing-professional.webp"`.
   With responsive `srcSet` on the hero, the preload MUST become `imageSrcSet` +
   `imageSizes` (React 18.3 renders the camelCase `imageSrcSet`/`imageSizes` props on
   `<link>` to lowercase `imagesrcset`/`imagesizes`) so the preloader fetches the SAME
   variant the `<source>` picks. Keep `href` set to a mid webp variant as a fallback
   for engines that ignore `imagesrcset`. This is the highest-risk part — a mismatch
   double-downloads and REGRESSES LCP.
5. The repo's `<picture>` prerenders correctly under streaming SSR (existing webp
   `<source srcSet>` already emits as `srcset` in dist HTML). Picture is pure markup,
   no Suspense concern. STATE.md note 260615-flf: AVIF already smaller than jpg for
   every base; webp already re-encoded small.
6. All 26 carousel/gallery project bases have .avif + .webp + .jpg siblings.
   `dte-roofing-job-site-columbus` (About) has all three. Hero has only webp + jpg.
   Headshots have webp + jpg (OUT of scope). Logos have webp + png (OUT of scope).
7. VERIFIED layout — `<WorkCarousel />` is at `Home.tsx:428` inside
   `<div className="max-w-4xl mx-auto mb-8">` within a `container mx-auto px-4` section:
   a SINGLE full-width column (max-w-4xl = 896px), NOT a left/right split. Slides are
   `basis-full`, so the rendered slide width = the container width (≤ ~896px), full
   viewport below that breakpoint.
</verified_facts>

<interfaces>
Current Picture props (extend, do not break):
```typescript
interface PictureProps {
  src: string; alt: string; className?: string;
  width?: number; height?: number;
  loading?: 'eager' | 'lazy';
  decoding?: 'async' | 'sync' | 'auto';
  fetchPriority?: 'high' | 'low' | 'auto';
  avif?: boolean; webp?: boolean;   // defaults true
}
// withExt(src, ext) swaps the extension: '/images/foo.jpg' -> '/images/foo.webp'
```
Current SEO preload (SEO.tsx:65-73) emits a single `<link rel=preload as=image href type fetchpriority>`.
optimize-images.mjs quality conventions: PHOTO_Q = { avif: 50, webp: 74, jpg: 78 };
hero job uses { webp: 60, jpg: 72 }. The required design specifies avif ~48 / webp ~62
/ jpg ~78 for the new variants — use those (avif:48, webp:62, jpg:78).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Variant generator script + typed manifest</name>
  <files>scripts/generate-responsive-variants.mjs, src/data/imageVariants.ts, package.json</files>
  <action>
Create `scripts/generate-responsive-variants.mjs` (ESM, sharp, network-free, &lt;500
lines, 2-space indent, matching `optimize-images.mjs` style — same `__dirname`/`ROOT`/`IMG`
setup, same `import sharp from 'sharp'`). Do NOT fetch anything from the network; read
existing local bases only.

Build the in-scope base list:
  - Parse `src/data/projects.ts` for every `image: '/images/<file>'` (reuse the regex
    shape from optimize-images.mjs; the 26 project slugs cover carousel + gallery + the
    project photos About reuses).
  - Add the hero base `hero-roofing-professional.jpg`.
  - Add the two About-only photos NOT in projects.ts: `dte-roofing-job-site-columbus.jpg`,
    `dte-roofing-completed-project-central-ohio.jpg`, and
    `dte-roofing-shingle-installation-columbus.jpg` (referenced in About.tsx job-site grid;
    `gahanna-premium-designer-shingles`, `dublin-premium-asphalt-shingles`,
    `powell-architectural-shingle-design` are already project slugs).
  - EXPLICITLY skip-list (never generate variants for): `donovan-davis-dte-roofing`,
    `mitchell-davis-dte-roofing`, `andrew-watts-dte-roofing`, and anything matching
    `/^DTE-Roofing-Logo/`. Filter the base list against this skip-list defensively even
    though they aren't in the in-scope list.

For each base: detect which modern sibling formats EXIST on disk (`.avif` and/or `.webp`)
plus always include `.jpg`. Read the base `.jpg` intrinsic width with sharp metadata.
For the ladder [400, 800, 1200, 1600], for each rung `w` where `w < intrinsicWidth`
(strictly less — the base file itself is the full-size top of the set; do NOT emit a
`-1200` for a 1200-wide hero), emit `slug-<w>.<fmt>` for each present format using
`resize({ width: w, height: w, fit: 'inside', withoutEnlargement: true })` exactly like
optimize-images.mjs (so portrait photos cap correctly), quality avif:48 / webp:62 / jpg:78,
`mozjpeg:true` for jpg, `.rotate()` to honor EXIF.

CRITICAL (verified_fact 3): after `toFile()`, read back `info.width`. Use `info.width`
as the descriptor — NOT the requested `w`. Guard with a per-base `Set` of emitted widths:
if `info.width` is already recorded OR `info.width !== w` (the rung clamped, e.g. a
900-wide portrait asked for 1200 came back 900), do NOT record that descriptor and delete
the duplicate file you just wrote (or skip writing it). Only record a variant width when
`info.width === w` AND `w < intrinsicWidth`. Net effect: the manifest and the on-disk
files agree exactly, and no `-1200.webp` that is actually 900px wide ever exists.

Write `src/data/imageVariants.ts` as a generated typed file (camelCase .ts per repo
convention, TS strict clean — no JSON import). Shape:
```
export const imageVariants: Record<string, { intrinsicWidth: number; widths: number[]; formats: string[] }> = { ... };
```
Keyed by the base public path of the .jpg (e.g. `/images/columbus-residential-roof-replacement.jpg`,
`/images/hero-roofing-professional.jpg`). `widths` = sorted unique emitted variant widths
(excluding the intrinsic/base width). `formats` = the present formats for that base
(e.g. `['avif','webp','jpg']` or hero `['webp','jpg']`). Prepend a
`// GENERATED by scripts/generate-responsive-variants.mjs — do not edit by hand.` header.
Make it idempotent and re-runnable (overwrite the .ts each run; re-runs must produce the
identical .ts byte-for-byte). Print a per-image summary line like optimize-images.mjs.

Add to package.json scripts: `"generate-responsive-variants": "node scripts/generate-responsive-variants.mjs"`.
Then RUN it once: `node scripts/generate-responsive-variants.mjs`.
  </action>
  <verify>
    <!-- Idempotent: a second run must leave imageVariants.ts byte-identical. -->
    <automated>node scripts/generate-responsive-variants.mjs && cp src/data/imageVariants.ts /tmp/iv1.ts && node scripts/generate-responsive-variants.mjs && diff -q src/data/imageVariants.ts /tmp/iv1.ts && echo "OK: idempotent"</automated>
  </verify>
  <verify>
    <!-- Correctness gate: every recorded variant width has a real file AND no file is upscaled. -->
    <automated>node -e '
const sharp=require("sharp");
const src=require("fs").readFileSync("src/data/imageVariants.ts","utf8");
const m=src.match(/imageVariants[^=]*=\s*(\{[\s\S]*\});/);
const obj=eval("("+m[1].replace(/\/\/.*$/gm,"")+")");
(async()=>{
  let bad=0;
  for(const [base,info] of Object.entries(obj)){
    const slug=base.replace(/^\/images\//,"").replace(/\.jpg$/,"");
    const meta=await sharp("public/images/"+slug+".jpg").metadata();
    if(meta.width!==info.intrinsicWidth){console.log("INTRINSIC MISMATCH",base,meta.width,info.intrinsicWidth);bad++;}
    for(const w of info.widths){
      if(w>=info.intrinsicWidth){console.log("UPSCALE/DUP DESCRIPTOR",base,w);bad++;}
      for(const f of info.formats){
        const p="public/images/"+slug+"-"+w+"."+(f==="jpg"?"jpg":f);
        if(!require("fs").existsSync(p)){console.log("MISSING FILE",p);bad++;continue;}
        const vm=await sharp(p).metadata();
        if(vm.width!==w){console.log("WIDTH LIE",p,"is",vm.width,"claims",w);bad++;}
      }
    }
  }
  if(bad){console.log("FAIL",bad);process.exit(1);}
  console.log("OK: all variants exist, no upscales, no width lies");
})();
'</automated>
  </verify>
  <done>
`scripts/generate-responsive-variants.mjs` and `src/data/imageVariants.ts` exist.
Running the script twice leaves the .ts byte-identical (idempotent). The manifest is
keyed by base .jpg path; every `widths[]` entry has a real file in all listed formats
whose actual width equals the descriptor and is strictly less than `intrinsicWidth`.
Headshots and logos have NO `-<w>` variants and NO manifest entries. The
`hero-roofing-professional.jpg` entry has `formats: ['webp','jpg']` and `widths`
containing only values &lt; 1200 (i.e. [400, 800]). `dte-roofing-job-site-columbus.jpg`
(500 wide) has `widths: [400]` only. npm script `generate-responsive-variants` added.
  </done>
</task>

<task type="auto">
  <name>Task 2: Responsive support in Picture.tsx + SEO.tsx</name>
  <files>src/components/Picture.tsx, src/components/SEO.tsx</files>
  <action>
**Picture.tsx** — add optional `sizes?: string` to `PictureProps`. Import
`{ imageVariants }` from `../data/imageVariants`.

Behavior gate: compute `const variant = sizes ? imageVariants[src] : undefined;`
(look up by the .jpg `src`). ONLY when `sizes` is provided AND `variant` exists, build
responsive output. Otherwise render the EXACT current markup (verified_fact: untouched
call sites must be byte-identical).

When responsive:
  - Add a helper `variantPath(src, w, ext)` turning `/images/foo.jpg` + 800 + 'webp'
    into `/images/foo-800.webp`.
  - For each format source emitted (`avif` when `avif!==false` && variant.formats includes
    'avif'; `webp` when `webp!==false` && includes 'webp'), build a `srcSet` listing each
    `variant.widths` entry as `variantPath(src,w,fmt) + " " + w + "w"` PLUS the full-size
    base as its intrinsic width: `withExt(src,fmt) + " " + variant.intrinsicWidth + "w"`.
    Emit `<source type="image/<fmt>" srcSet={srcSet} sizes={sizes} />`.
  - Fallback `<img>`: keep `src={src}` (full-size jpg) as the default, ADD
    `srcSet={jpgSrcSet}` (jpg variants + base intrinsic, same construction) and
    `sizes={sizes}`. Preserve every existing `<img>` attribute (className, width, height,
    loading, decoding, fetchpriority).
  - Preserve the existing source ordering (avif first, then webp, then img) and the
    `<picture className="contents">` wrapper unchanged.

Keep it under the repo's 500-line rule (~80 lines). 2-space indent, explicit return type
`: JSX.Element`. Directive prose only — implement in the .tsx, no fenced code in this plan.

**SEO.tsx** — make the hero preload responsive. Import `{ imageVariants }` from
`../data/imageVariants`. Add new prop `preloadImageSizes?: string` (default `"100vw"`);
keep existing `preloadImage?: string`. When `preloadImage` is set:
  - Normalize to the manifest key: `const key = preloadImage.replace(/\.(webp|avif|jpg)$/i,'.jpg')`
    and `const pv = imageVariants[key]` (hero preload passes the .webp path).
  - If `pv` exists and `pv.formats` includes 'webp': build a webp `imageSrcSet` from
    `pv.widths` (as `variantPath(key,w,'webp') + " " + w + "w"`) PLUS the base webp at
    `pv.intrinsicWidth`w. Set `imageSizes={preloadImageSizes}`. Set `href` to a MID webp
    variant (`pv.widths[Math.floor(pv.widths.length/2)]` if any, else the base webp) as a
    fallback for engines that ignore `imagesrcset`. Keep `type="image/webp"` and
    `fetchpriority:'high'`. Pass `imageSrcSet`/`imageSizes` as React camelCase props on
    the `<link>` (React 18.3 lowercases them to `imagesrcset`/`imagesizes`).
  - If `pv` is absent (no manifest entry), fall back to the CURRENT single-file preload
    behavior unchanged.

VERIFY IN THE PRERENDERED dist HTML that the head preload renders as lowercase
`imagesrcset`/`imagesizes`. If react-helmet-async drops the camelCase props (renders
nothing or strips them), FALL BACK to injecting the responsive `<link>` via the prerender
path and FLAG it in the SUMMARY. Do NOT ship a preload that fetches one fixed file while
the `<source>` fetches a different variant.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5 && node -e '
const fs=require("fs");
const home=fs.readFileSync("dist/index.html","utf8");
const hasImgSrcset=/imagesrcset=/i.test(home);
const hasImgSizes=/imagesizes=/i.test(home);
console.log("hero preload imagesrcset:",hasImgSrcset,"imagesizes:",hasImgSizes);
if(!hasImgSrcset||!hasImgSizes){console.log("FAIL: hero preload not responsive (Helmet may have dropped camelCase props — FLAG and fall back to raw <link>)");process.exit(1);}
console.log("OK");
'</automated>
  </verify>
  <done>
`npm run build` prerenders all 41 routes clean. `Picture` is backward compatible: with no
`sizes` prop it emits the identical single-source markup as before; with `sizes` + a
manifest entry it emits per-format `<source srcSet sizes>`. `dist/index.html` head
contains a hero preload with lowercase `imagesrcset` AND `imagesizes` referencing the hero
webp `-<w>` variants, with `href` set to a mid webp variant. If Helmet drops the camelCase
props, the executor FLAGS it and falls back to emitting the preload via the prerender path
(never a single fixed file that mismatches the `<source>`).
  </done>
</task>

<task type="auto">
  <name>Task 3: Wire layout-derived sizes at in-scope call sites</name>
  <files>src/pages/Home.tsx, src/components/WorkCarousel.tsx, src/pages/Gallery.tsx, src/pages/About.tsx</files>
  <action>
Add a `sizes` prop (layout-derived) to each IN-SCOPE `<Picture>` only. Do NOT touch the
About headshots (lines ~239/263/287) or any logo Picture.

  - **Home.tsx hero** (line ~36): add `sizes="100vw"` to the hero `<Picture>` (full-bleed
    `absolute inset-0`). On the `<SEO>` call (line ~22-28) add `preloadImageSizes="100vw"`
    alongside the existing `preloadImage="/images/hero-roofing-professional.webp"`.
  - **WorkCarousel.tsx** (line ~32): VERIFIED (fact 7) — the carousel lives in a single
    `max-w-4xl mx-auto` (896px) full-width container, slides `basis-full`. So the rendered
    slide width is the viewport below ~896px and caps near 896px above. Use
    `sizes="(min-width: 896px) 896px, 100vw"`. (At ≥896px the 896px declaration selects the
    800w variant; below it, 100vw selects 400/800 by viewport. Prefer slightly-too-large
    over too-small.)
  - **Gallery.tsx grid** (line ~141): grid is `grid-cols-1 md:grid-cols-2 lg:grid-cols-3`
    inside `max-w-7xl` with `gap-6/8`. Use
    `sizes="(min-width:1024px) 33vw, (min-width:768px) 50vw, 100vw"`. Do NOT add sizes to
    the lightbox `<Picture>` at line ~256 (it renders `object-contain max-h-[75vh]` — a
    full-resolution view; leave it serving the base image).
  - **About.tsx job-site grid** (lines ~324-350): grid is
    `grid-cols-1 sm:grid-cols-2 lg:grid-cols-3`. Use
    `sizes="(min-width:1024px) 33vw, (min-width:640px) 50vw, 100vw"` on each of the six
    job-site `<Picture>` elements (gahanna-premium-designer-shingles, dte-roofing-job-site-columbus,
    dublin-premium-asphalt-shingles, powell-architectural-shingle-design,
    dte-roofing-shingle-installation-columbus, dte-roofing-completed-project-central-ohio).
    Leave the three headshots and any logo untouched.

Keep all other props on every Picture unchanged.
  </action>
  <verify>
    <!-- Build clean + prerendered HTML shows srcset on in-scope images and NOT on untouched ones. -->
    <automated>npm run build 2>&1 | tail -3 && node -e '
const fs=require("fs");
const must=p=>fs.readFileSync(p,"utf8");
const home=must("dist/index.html");
const gallery=must("dist/gallery/index.html");
const about=must("dist/about/index.html");
let fail=0;
// In-scope: hero webp variant + sizes present on home
if(!/hero-roofing-professional-\d+\.webp/.test(home)){console.log("FAIL home: no hero webp variant in srcset");fail++;}
if(!/sizes="100vw"/.test(home)){console.log("FAIL home: hero sizes=100vw missing");fail++;}
// In-scope: gallery grid images carry a -<w> variant descriptor + sizes
if(!/-\d+\.webp \d+w/.test(gallery)){console.log("FAIL gallery: no responsive webp srcset");fail++;}
if(!/min-width:1024px\) 33vw/.test(gallery)){console.log("FAIL gallery: grid sizes missing");fail++;}
// In-scope: about job-site grid responsive
if(!/-\d+\.webp \d+w/.test(about)){console.log("FAIL about: no responsive webp srcset");fail++;}
// OUT of scope: headshot must NOT gain a srcset variant
if(/(donovan|mitchell|andrew)-davis?-dte-roofing-\d+\.webp/.test(about)){console.log("FAIL about: headshot got a variant — should be untouched");fail++;}
if(fail){console.log("FAIL",fail);process.exit(1);}
console.log("OK: in-scope responsive, headshots untouched");
'</automated>
  </verify>
  <verify>
    <!-- No 404s: every srcset URL referenced in the home/gallery/about HTML resolves to a real public/images file. -->
    <automated>node -e '
const fs=require("fs");
const pages=["dist/index.html","dist/gallery/index.html","dist/about/index.html"];
const urls=new Set();
for(const p of pages){const h=fs.readFileSync(p,"utf8");for(const m of h.matchAll(/\/images\/[A-Za-z0-9._-]+\.(?:avif|webp|jpg|png)/g))urls.add(m[0]);}
let miss=0;
for(const u of urls){const f="public"+u;if(!fs.existsSync(f)){console.log("404",u);miss++;}}
if(miss){console.log("FAIL: "+miss+" referenced image(s) missing");process.exit(1);}
console.log("OK: all "+urls.size+" referenced images exist");
'</automated>
  </verify>
  <done>
`npm run build` clean (41 routes). Prerendered `dist/index.html` hero `<picture>` emits
`srcSet` with hero `-<w>.webp` variants + `sizes="100vw"`, and the head preload uses
`imagesrcset`/`imagesizes` referencing the SAME hero webp variants (no double-download).
Gallery grid and About job-site grid emit per-format responsive `<source srcSet sizes>`.
The three About headshots and all logos emit NO `-<w>` variants (byte-identical to
before). Every image URL referenced anywhere in the prerendered HTML resolves to a real
file in `public/images/` (no 404s).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build script → filesystem | `generate-responsive-variants.mjs` reads/writes `public/images` and `src/data` |
| browser → CDN images | client fetches `/images/*-<w>.<fmt>` chosen from srcSet |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-g1y-01 | Tampering | srcSet width descriptors | mitigate | Task 1 verify reads back `info.width` and asserts each emitted file's real width == descriptor and < intrinsic (no upscale, no lie) |
| T-g1y-02 | Information Disclosure | hero preload mismatch | mitigate | Task 2 verify asserts head `imagesrcset` references the SAME hero webp variants the `<source>` picks — prevents double-download / LCP regression |
| T-g1y-03 | Denial of Service | broken image (404) | mitigate | Task 3 verify cross-checks every `/images/...` URL in prerendered HTML against `public/images/` |
| T-g1y-04 | Tampering | npm/devDep installs | accept | No new deps — sharp ^0.34.5 already a devDependency; script is network-free; no install step |
| T-g1y-05 | Repudiation | untouched call sites regress | mitigate | Picture is backward-compatible (no `sizes` → identical markup); Task 3 verify asserts headshots gain no variant |
</threat_model>

<verification>
Overall phase verification (run after Task 3):

1. `npm run build` — generate-sitemap → vite build → ssr build → prerender of 41 routes,
   exits 0. (`typecheck`/`lint` are PRE-EXISTING RED — ignore; not regressions.)
2. Hero match: `dist/index.html` `<picture>` for the hero emits a webp `<source srcSet>`
   with `hero-roofing-professional-<w>.webp <w>w` entries + `sizes="100vw"`, AND the
   `<head>` preload uses `imagesrcset`/`imagesizes` referencing those same variants with
   `href` set to a mid webp variant (no double-download).
3. No upscaling: every `-<w>.<fmt>` file's actual width == `<w>` and `< intrinsic`
   (Task 1 correctness gate).
4. No 404s: every `/images/...` URL in the prerendered home/gallery/about HTML resolves
   to a real file (Task 3 second gate).
5. Untouched: the three About headshots and all logos emit NO `-<w>` variants and render
   byte-identically (Task 3 first gate negative assertion).
6. Do NOT run `optimize-images.mjs`. Do NOT push to any remote.
</verification>

<success_criteria>
- New `scripts/generate-responsive-variants.mjs` (sharp, ESM, network-free, idempotent,
  &lt;500 lines, 2-space indent) + `generate-responsive-variants` npm script, run once.
- Generated `src/data/imageVariants.ts` typed manifest keyed by base .jpg path, recording
  only REAL emitted widths per base+format; headshots/logos absent.
- `Picture` emits responsive `<source srcSet sizes>` ONLY with a `sizes` prop + manifest
  entry; identical single-source markup otherwise.
- `SEO` hero preload is responsive (`imageSrcSet`/`imageSizes`) and matches the `<source>`.
- In-scope call sites (hero, WorkCarousel, Gallery grid, About job-site grid) pass
  layout-derived `sizes`; headshots/logos/lightbox untouched.
- `npm run build` clean (41 routes); no upscaled variants; no 404s; no remote push.
</success_criteria>

<output>
Create `.planning/quick/260615-g1y-add-responsive-srcset-sizes-to-large-ima/260615-g1y-SUMMARY.md` when done.
</output>
