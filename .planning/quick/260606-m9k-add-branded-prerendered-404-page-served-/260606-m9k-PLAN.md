---
quick_id: 260606-m9k
description: Add branded prerendered 404 page served by Vercel with 404 status
date: 2026-06-06
mode: quick (inline — gsd-* subagent types unavailable in this harness)
---

# Quick Task 260606-m9k — Plan

## Background

Unmatched paths currently get Vercel's plain-text `NOT_FOUND` body (correct 404 status, zero UX). Audit finding #2 (2026-06-06). Vercel static deployments serve a root-level `dist/404.html` with status 404 for unmatched routes.

## Tasks

### Task 1 — Branded NotFound page, prerendered to dist/404.html

**Files:** `src/pages/NotFound.tsx` (new), `src/App.tsx`, `src/components/SEO.tsx`, `src/routes.config.mjs`, `scripts/prerender.mjs`

1. `NotFound.tsx`: design-matched 404 (charcoal-900 hero, primary-700 CTAs) with links to Home/Services/Service Areas/Blog/Contact + phone. `SEO` with `noindex` (new optional prop emitting `<meta name="robots" content="noindex">`); no canonical.
2. `App.tsx`: lazy `NotFound`, `<Route path="*" element={<NotFound />} />` (client-side coverage for bad internal links).
3. `routes.config.mjs`: export `NOT_FOUND_ROUTE = { path: '/404', source: 'src/pages/NotFound.tsx' }` — separate from `ROUTES` so the sitemap (which maps all of ROUTES) is untouched.
4. `prerender.mjs`: after the main loop, render `'/404'` (catch-all matches) and write to `dist/404.html` (root file, not `404/index.html`), with modulepreload hints from the NotFound chunk.

**Verify:** build → `dist/404.html` exists, contains branded markup + noindex; sitemap still 41 URLs without /404; local preview serves it for bogus paths (vite preview may differ from Vercel — production curl is the real gate: bogus URL → 404 status + branded HTML).

**Done:** Bogus URLs on production return 404 status with a branded, navigable page.

## must_haves

- dist/404.html prerendered, noindex, not in sitemap
- Production: unmatched path → HTTP 404 + branded body with internal links
- No URL/content/NAP changes elsewhere
