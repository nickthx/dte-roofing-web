---
id: 260814-k1s
description: Delete verified-dead public and src files
date: 2026-08-14
mode: quick
---

# Quick Task 260814-k1s: Delete verified-dead public and src files

Remove nine files/directories that no shipping code, built output, or platform
config references. Pure deletion — no behavior change intended, and none possible
if the reference audit below holds.

## Deletion set

### Group 1 — `public/` (shipped to production, unreferenced)

| File | Why dead |
|---|---|
| `public/favicon-preview.html` | Dev-only preview page. Publicly reachable at `/favicon-preview.html`. Nothing links to it. |
| `public/favicon-16x16.svg` | Referenced **only** by `favicon-preview.html` (also deleted). Not in `index.html`, `site.webmanifest`, or `browserconfig.xml`. |
| `public/favicon-32x32.svg` | Same as above. |
| `public/browserconfig.xml` | Windows tile config. `index.html` has no `msapplication-config` meta, so nothing ever fetches it. Only self-references. |
| `public/data/blog-posts.json` | Contains literally `[]`. Supabase-era artifact; the blog now renders from `src/data/blogPosts.tsx`. Zero fetches in `src/`. |
| `public/roofle-embed.html` | Standalone Roofle iframe page. Zero references in `src/`, `index.html`, `vercel.json`, or `dist/`. The live widget loads via `RoofleSlideout.tsx` + the CDN script. |

Retained deliberately: `favicon.svg` (referenced by `site.webmanifest`),
`apple-touch-icon.svg` (referenced by `site.webmanifest`), `dte_favicon.png`
(referenced by `index.html`), both `google*.html` GSC ownership tokens.

### Group 2 — dead source

| File | Why dead |
|---|---|
| `src/components/seo/SeoSchema.tsx` | Zero imports anywhere in `src/`. Schema is emitted by `SEO.tsx` + `SchemaMarkup.tsx`. |
| `src/routes.config.ts` | A TypeScript re-export wrapper around `routes.config.mjs`. Every consumer (`prerender.mjs`, `generate-sitemap.mjs`, `verify-phase-05.mjs`) imports the `.mjs` directly. Nothing imports this file. |
| `dead-code/` | `GutterServices.tsx`, `RoofMaintenance.tsx`, `README.md`. Quarantined 2026-05-03; outside `tsconfig.app.json`'s `include: ["src"]`, so never compiled. The `/services/gutter-services` and `/services/roof-maintenance` 301s live in `vercel.json`, not here. |

## Reference audit (performed before planning)

- Repo-wide grep for each basename across all files excluding `node_modules`,
  `.git`, `dist`, `.vercel` — every hit landed in `.planning/` historical docs,
  `repo-context.txt`, or the file itself.
- `dist/` (last build) grepped: only `dist/favicon-preview.html` referenced the
  16x16/32x32 SVGs, and only `dist/browserconfig.xml` referenced itself.
- `index.html`, `public/site.webmanifest`, `vercel.json` read in full — no path
  in the deletion set appears in any of them.
- `tsconfig.app.json` confirmed `include: ["src"]`, so `dead-code/` is already
  out of the typecheck graph.

## Tasks

1. **Delete the files.** `git rm -r` the nine paths. Single atomic commit.
   - verify: `git status` shows exactly 9 path deletions, nothing else staged
   - done: working tree clean of these paths

2. **Verify the build.** `npm run build` (sitemap → client → SSR → prerender).
   - verify: build exits 0; prerender emits 41 routes
   - done: `dist/` regenerated with no missing-module or missing-asset errors

## Out of scope

- `public/dte_favicon.png` (344 KB) is oversized but **referenced** — replacing it
  is a separate task.
- Root clutter (`repo-context.txt`, `.docx` audits, `.bolt/`, `memory/`,
  one-off `verify-phase-0*.sh`) — separately identified, not part of this task.
