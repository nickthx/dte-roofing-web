---
phase: 260413-tz5
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - vercel.json
  - memory/audits/technical-seo-checker/2026-04-13-dteroofingllc-technical-seo.md
autonomous: true
requirements:
  - SEO-AUDIT-BLOG-REWRITE
  - SEO-AUDIT-308-301

must_haves:
  truths:
    - "vercel.json no longer contains the dead /blog/:slug rewrite"
    - "vercel.json remains valid JSON that Vercel can parse on next deploy"
    - "Audit memory file reflects both items as closed with correct disposition (Resolved vs Accepted)"
  artifacts:
    - path: "vercel.json"
      provides: "Vercel deployment config without obsolete blog rewrite"
      contains: "redirects"
    - path: "memory/audits/technical-seo-checker/2026-04-13-dteroofingllc-technical-seo.md"
      provides: "Updated audit with Closed Items section"
      contains: "Closed Items"
  key_links:
    - from: "vercel.json"
      to: "Vercel platform"
      via: "JSON parse on deploy"
      pattern: "valid JSON, no rewrites key (or empty)"
    - from: "audit .md"
      to: "future audit reviews"
      via: "Closed Items section"
      pattern: "Resolved \\(rewrite removed\\)|Accepted \\(working as intended"
---

<objective>
Remove the dead `/blog/:slug` rewrite from vercel.json and update the 2026-04-13 technical SEO audit memory to reflect two items as closed.

Purpose:
- The rewrite at vercel.json:33-35 maps `/blog/:slug` to `/index.html`, but no blog posts exist (`public/data/blog-posts.json` is `[]`) and `scripts/prerender.mjs:22` explicitly skips dynamic routes. The rewrite is broken AND obsolete. When blog activates, prerender.mjs will be extended to generate per-slug pages — not rely on SPA fallback.
- The 308-vs-301 audit item needs to be marked "Accepted" — Vercel's platform-level apex→www redirect sends 308 which is SEO-equivalent to 301 per Google. No code change warranted.

Output:
- vercel.json with `rewrites` key removed entirely (cleaner than `"rewrites": []`)
- Audit memory file with both findings moved to a new "Closed Items" section
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@vercel.json
@memory/audits/technical-seo-checker/2026-04-13-dteroofingllc-technical-seo.md

<interfaces>
Current vercel.json structure (lines 1-36):
- redirects: array of 9 redirect rules (KEEP all)
- headers: security headers block (KEEP)
- cleanUrls: true (KEEP)
- trailingSlash: false (KEEP)
- rewrites: array with single `/blog/:slug` → `/index.html` rule (REMOVE entire key)

Current audit file structure:
- Frontmatter (name, description, type, date, score)
- Scores table
- Verified Live section
- "## Remaining Issues" section containing:
  - P1 — Missing social/share tags (KEEP in Remaining)
  - P1 — `/blog/:slug` rewrite not serving (MOVE to Closed)
  - P2 — Naked→www redirect sends 308 (MOVE to Closed)
  - P2 — Polish (KEEP in Remaining)
- "## What's Already Solved" section
- "## Source" section
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove dead /blog/:slug rewrite from vercel.json</name>
  <files>vercel.json</files>
  <action>
    Edit vercel.json to remove the `rewrites` key entirely (lines 33-35 plus the comma after `trailingSlash: false`).

    Specifically:
    1. Change line 32 from `"trailingSlash": false,` to `"trailingSlash": false` (drop trailing comma — it's now the last property)
    2. Remove lines 33-35 (the entire `"rewrites": [...]` block)
    3. The closing `}` on line 36 becomes the new final line

    Final structure: redirects, headers, cleanUrls, trailingSlash — no rewrites key.

    Rationale (do not include as a comment in the file — this is context for the change):
    - `public/data/blog-posts.json` is `[]`, no posts exist
    - `scripts/prerender.mjs:22` has `if (route.includes(':')) continue;` — dynamic routes are intentionally skipped
    - When blog activates, extend `scripts/prerender.mjs` to generate per-slug static pages from blog-posts.json; do NOT reintroduce the SPA fallback rewrite
    - Removing the key entirely (vs leaving `"rewrites": []`) is cleaner and matches Vercel's convention of omitting empty config blocks

    Do NOT touch the `redirects` array, `headers` array, `cleanUrls`, or `trailingSlash`.
  </action>
  <verify>
    <automated>node -e "JSON.parse(require('fs').readFileSync('vercel.json','utf8')); const c = JSON.parse(require('fs').readFileSync('vercel.json','utf8')); if (c.rewrites) { console.error('FAIL: rewrites key still present'); process.exit(1); } if (!c.redirects || c.redirects.length !== 9) { console.error('FAIL: redirects array modified'); process.exit(1); } if (!c.headers || !c.cleanUrls || c.trailingSlash !== false) { console.error('FAIL: other keys modified'); process.exit(1); } console.log('OK: vercel.json valid, rewrites removed, other keys intact');"</automated>
  </verify>
  <done>
    vercel.json parses as valid JSON; `rewrites` key is absent; `redirects` (9 entries), `headers`, `cleanUrls: true`, `trailingSlash: false` all unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 2: Move two audit findings to Closed Items section</name>
  <files>memory/audits/technical-seo-checker/2026-04-13-dteroofingllc-technical-seo.md</files>
  <action>
    Restructure the audit file so the `/blog/:slug` and 308-vs-301 findings move out of "Remaining Issues" into a new "Closed Items" section.

    Steps:

    1. Update the top-level `**Verdict:**` sentence (line 11) to reflect current reality. Change:
       > "Remaining gaps are narrow: missing social-share meta tags, an unresolved `/blog/:slug` rewrite, and a 308-vs-301 canonicalization detail."

       To:
       > "Remaining gaps are narrow: missing social-share meta tags and polish items (LCP preload, sitemap image entries)."

    2. In the `## Remaining Issues` section, DELETE these two blocks entirely:
       - `### P1 — `/blog/:slug` rewrite not serving` (lines 45-46)
       - `### P2 — Naked→www redirect sends 308` (lines 48-49)

       KEEP:
       - `### P1 — Missing social/share tags` (lines 36-43)
       - `### P2 — Polish` (lines 51-53)

    3. Insert a new `## Closed Items` section BETWEEN `## Remaining Issues` and `## What's Already Solved`. Use this exact content:

       ```markdown
       ## Closed Items

       ### ✅ `/blog/:slug` rewrite — Resolved (rewrite removed) 2026-04-13
       `vercel.json` previously declared `{ "source": "/blog/:slug", "destination": "/index.html" }` but the rewrite was both broken (404s) and obsolete:
       - `public/data/blog-posts.json` is `[]` — no posts exist
       - `scripts/prerender.mjs:22` has `if (route.includes(':')) continue;` — dynamic routes are intentionally skipped
       - SPA fallback would not serve meaningful content anyway (prerender pipeline is the intended render path)

       **Resolution:** `rewrites` key removed entirely from `vercel.json` (quick task `260413-tz5`). When blog activates, extend `scripts/prerender.mjs` to generate per-slug static pages from `blog-posts.json` — do NOT reintroduce the SPA fallback.

       ### ✅ Naked→www sends 308 — Accepted (working as intended) 2026-04-13
       `vercel.json:14` declares `statusCode: 301` but Vercel sends **308 Permanent Redirect**. Root cause: Vercel's platform-level apex→primary-domain redirect takes precedence over `vercel.json` redirects.

       **Disposition:** Accepted. No code change.
       - Google treats 301 and 308 identically for SEO — zero ranking impact
       - 308 is semantically more correct (preserves HTTP method on redirect)
       - Overriding the platform default would require removing the apex domain from Vercel project settings, which is not worth the operational risk for a semantics-only improvement
       ```

    4. Do NOT modify frontmatter, Scores table, Verified Live section, What's Already Solved section, or Source section.

    Use the Edit tool for each surgical change — do NOT rewrite the whole file.
  </action>
  <verify>
    <automated>node -e "const c = require('fs').readFileSync('memory/audits/technical-seo-checker/2026-04-13-dteroofingllc-technical-seo.md','utf8'); const checks = [['Closed Items section exists', c.includes('## Closed Items')], ['Resolved marker for blog rewrite', c.includes('Resolved (rewrite removed)')], ['Accepted marker for 308', c.includes('Accepted (working as intended)')], ['Old blog item removed from Remaining', !c.includes('### P1 — `/blog/:slug` rewrite not serving')], ['Old 308 item removed from Remaining', !c.includes('### P2 — Naked→www redirect sends 308')], ['Social tags item still in Remaining', c.includes('### P1 — Missing social/share tags')], ['Polish item still in Remaining', c.includes('### P2 — Polish')], ['What\\'s Already Solved preserved', c.includes(\"## What's Already Solved\")], ['Source section preserved', c.includes('## Source')]]; let failed = 0; for (const [name, ok] of checks) { if (!ok) { console.error('FAIL:', name); failed++; } else { console.log('OK:', name); } } process.exit(failed ? 1 : 0);"</automated>
  </verify>
  <done>
    Audit file contains `## Closed Items` section with both findings (one marked "Resolved (rewrite removed)", one marked "Accepted (working as intended)"). `## Remaining Issues` retains only the social-share tags and Polish items. Frontmatter, Scores, Verified Live, What's Already Solved, and Source sections unchanged.
  </done>
</task>

</tasks>

<verification>
Both tasks have automated verification baked into `<verify>`. After completion:

1. `vercel.json` parses, has no `rewrites` key, other config preserved (Task 1 verify command)
2. Audit file has `Closed Items` section with correct dispositions, old entries removed from `Remaining Issues` (Task 2 verify command)

No deploy, build, or runtime verification needed — this is a config + docs change. Vercel picks up vercel.json changes on the next deploy automatically.
</verification>

<success_criteria>
- vercel.json no longer contains `/blog/:slug` rewrite (key removed entirely)
- vercel.json is valid JSON
- All other vercel.json config (redirects, headers, cleanUrls, trailingSlash) unchanged
- Audit memory file has new `## Closed Items` section with both findings
- `/blog/:slug` finding marked "Resolved (rewrite removed)" with reference to this quick task
- 308-vs-301 finding marked "Accepted (working as intended)" with rationale
- Verdict line updated to remove references to now-closed items
</success_criteria>

<output>
After completion, create `.planning/quick/260413-tz5-remove-dead-blog-slug-rewrite-from-verce/260413-tz5-01-SUMMARY.md` documenting:
- Files changed (vercel.json, audit .md)
- What was removed from vercel.json
- What was moved to Closed Items
- Next-time reminder: when blog activates, extend `scripts/prerender.mjs` to generate per-slug pages from `blog-posts.json` — do NOT reintroduce a SPA fallback rewrite
</output>
