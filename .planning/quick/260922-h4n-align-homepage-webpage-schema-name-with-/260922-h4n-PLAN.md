---
phase: quick-260922-h4n
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/Home.tsx
autonomous: true
requirements: [QUICK-260922-h4n]

must_haves:
  truths:
    - "The homepage WebPage JSON-LD `name` is the exact string 'Roof Repair and Replacement in Columbus, OH | DTE Roofing'"
    - "The old schema name 'DTE Roofing - Columbus's Highest-Rated Roofing Contractor' no longer appears anywhere in the prerendered homepage HTML"
    - "The homepage <title> and og:title are unchanged"
    - "The WebPage schema `description` (pageDescription) is unchanged"
  artifacts:
    - path: "src/pages/Home.tsx"
      provides: "SchemaMarkup pageTitle prop matching the SEO title prop"
      contains: "pageTitle=\"Roof Repair and Replacement in Columbus, OH | DTE Roofing\""
  key_links:
    - from: "src/pages/Home.tsx"
      to: "src/components/SchemaMarkup.tsx generateWebPageSchema()"
      via: "pageTitle prop -> schema.name"
      pattern: "pageTitle=\"Roof Repair and Replacement in Columbus, OH \\| DTE Roofing\""
---

<objective>
Align the homepage WebPage JSON-LD `name` with the homepage `<title>` tag so search engines see one consistent page title signal.

Purpose: The live homepage currently emits two different page names — `<title>`/`og:title` say "Roof Repair and Replacement in Columbus, OH | DTE Roofing" while the WebPage schema `name` says "DTE Roofing - Columbus's Highest-Rated Roofing Contractor". Mismatched title signals weaken the page's keyword association.
Output: A one-line prop change in `src/pages/Home.tsx`, verified through the production build's prerendered HTML.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md

@src/pages/Home.tsx
@src/components/SchemaMarkup.tsx
</context>

<interfaces>
<!-- Contracts the executor needs. No codebase exploration required. -->

From src/components/SchemaMarkup.tsx — the WebPage generator consumes `pageTitle` directly as `name`:

```typescript
const generateWebPageSchema = () => {
  if (!pageUrl) return null;
  return {
    '@context': 'https://schema.org',
    '@type': 'WebPage',
    '@id': `${pageUrl}#webpage`,
    url: pageUrl,
    name: pageTitle || BUSINESS_INFO.name,
    description: pageDescription,
    // ...
  };
};
```

Note: `generateBreadcrumbSchema()` early-returns `null` when `type === 'home'`, so changing `pageTitle` on the homepage affects ONLY the WebPage `name`. No breadcrumb or LocalBusiness field is touched.

Current state of src/pages/Home.tsx lines 22-35 (the only two props in play):
- Line 23: `title="Roof Repair and Replacement in Columbus, OH | DTE Roofing"` (SEO component — DO NOT CHANGE)
- Line 32: `pageTitle="DTE Roofing - Columbus's Highest-Rated Roofing Contractor"` (SchemaMarkup — THE ONLY LINE TO CHANGE)

Verified by grep: the string `Highest-Rated` occurs exactly once in the entire `src/` tree, at Home.tsx:32. It appears in no page copy, so removing it from the build output is a clean signal.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Match SchemaMarkup pageTitle to the SEO title on the homepage</name>
  <files>src/pages/Home.tsx</files>
  <action>Edit line 32 only. Replace the `pageTitle` prop value on the `<SchemaMarkup type="home" ...>` element so it is character-for-character identical to the `title` prop passed to `<SEO>` on line 23: `Roof Repair and Replacement in Columbus, OH | DTE Roofing`. Preserve the existing double-quoted JSX string-literal form and the surrounding indentation. Do NOT modify the `pageDescription`, `pageUrl`, or `type` props. Do NOT modify the `<SEO>` component's props (title, description, keywords, canonical, preloadImage). Do NOT touch any page copy, NAP details, or URLs elsewhere in the file. Do not edit `src/components/SchemaMarkup.tsx` — the component already maps `pageTitle` to the WebPage `name` correctly.</action>
  <verify>
    <automated>npm run build && grep -o '"name":"Roof Repair and Replacement in Columbus, OH | DTE Roofing"' dist/index.html | wc -l</automated>
    <automated>grep -c "Highest-Rated" dist/index.html</automated>
    <automated>grep -o "<title>Roof Repair and Replacement in Columbus, OH | DTE Roofing</title>" dist/index.html</automated>
  </verify>
  <done>
    `npm run build` exits 0. The first grep prints `1` (the WebPage schema name is present exactly once). The second grep prints `0` (the old name string is fully gone from the prerendered homepage). The third grep matches, confirming the `<title>` tag was not collaterally changed. `git diff --stat` shows exactly one file changed with 1 insertion and 1 deletion.
  </done>
</task>

</tasks>

<verification>
Build-output gates (this repo's verification gate is `npm run build`; `npm run typecheck` and `npm run lint` are pre-existing red and MUST NOT be used as pass/fail gates):

1. `npm run build` completes successfully.
2. `grep -o '"name":"Roof Repair and Replacement in Columbus, OH | DTE Roofing"' dist/index.html | wc -l` → `1`
3. `grep -c "Highest-Rated" dist/index.html` → `0`
4. `grep -o "<title>Roof Repair and Replacement in Columbus, OH | DTE Roofing</title>" dist/index.html` → one match (title tag untouched)
5. `git diff --stat src/pages/Home.tsx` → `1 file changed, 1 insertion(+), 1 deletion(-)`

Grep note: `dist/index.html` is emitted as a single line, so `grep -c` reports matching *lines*, not occurrences — that is why gate 2 pipes `grep -o` through `wc -l` to count actual occurrences. Gate 3's `grep -c` returning `0` is still a valid absence check.
</verification>

<success_criteria>
- The homepage WebPage JSON-LD `name` equals the `<title>` tag string exactly.
- No occurrence of "Highest-Rated" remains in the prerendered homepage.
- Diff is limited to one line in `src/pages/Home.tsx`; no content, NAP, URL, or description changes.
- `npm run build` succeeds.
</success_criteria>

<output>
Create `.planning/quick/260922-h4n-align-homepage-webpage-schema-name-with-/260922-h4n-SUMMARY.md` when done
</output>
