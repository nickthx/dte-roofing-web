---
phase: 01-data-architecture-schema-fixes
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/pages/Locations.tsx
  - src/pages/locations/Columbus.tsx
  - src/pages/locations/Hilliard.tsx
  - src/pages/locations/Dublin.tsx
  - src/pages/locations/NewAlbany.tsx
  - src/pages/locations/UpperArlington.tsx
  - src/pages/locations/Westerville.tsx
  - src/pages/locations/Gahanna.tsx
  - src/pages/locations/Reynoldsburg.tsx
  - src/pages/locations/GroveCity.tsx
  - src/pages/locations/Pickerington.tsx
  - src/pages/locations/Worthington.tsx
  - src/pages/locations/Delaware.tsx
  - src/pages/locations/Powell.tsx
autonomous: true
requirements:
  - SCHEMA-01
  - SCHEMA-08
  - HUB-01
  - HUB-02

must_haves:
  truths:
    - "Hub page H1 reads 'Areas We Serve in Central Ohio'"
    - "Hub page meta title includes 'Areas We Serve in Central Ohio'"
    - "Hub page SchemaMarkup uses type='hub' and generates valid RoofingContractor JSON-LD"
    - "All 13 location subpages pass locationSlug to SchemaMarkup"
    - "Each subpage's locationSlug matches its URL slug"
    - "TypeScript compiles and dev server renders without errors"
  artifacts:
    - path: "src/pages/Locations.tsx"
      provides: "Hub page with fixed H1, meta title, and hub schema"
      contains: "Areas We Serve in Central Ohio"
    - path: "src/pages/locations/Columbus.tsx"
      provides: "Columbus subpage with locationSlug='columbus'"
      contains: "locationSlug"
    - path: "src/pages/locations/Powell.tsx"
      provides: "Powell subpage with locationSlug='powell'"
      contains: "locationSlug"
  key_links:
    - from: "src/pages/Locations.tsx"
      to: "src/components/SchemaMarkup.tsx"
      via: "type='hub' prop"
      pattern: "type=\"hub\""
    - from: "src/pages/locations/Columbus.tsx"
      to: "src/components/SchemaMarkup.tsx"
      via: "locationSlug='columbus' prop"
      pattern: "locationSlug=\"columbus\""
---

<objective>
Wire all page components to the refactored SchemaMarkup: fix the hub page H1/title/schema and add locationSlug to all 13 subpages.

Purpose: Plan 01 built the engine (locations.ts + SchemaMarkup refactor). This plan connects every page to that engine so each page produces correct, unique JSON-LD schema.

Output: Updated Locations.tsx (hub) and all 13 location subpage files.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-data-architecture-schema-fixes/01-RESEARCH.md
@.planning/phases/01-data-architecture-schema-fixes/01-01-SUMMARY.md

<interfaces>
<!-- SchemaMarkup interface after Plan 01 refactor -->
From src/components/SchemaMarkup.tsx (post Plan 01):
```typescript
interface SchemaMarkupProps {
  type: 'home' | 'service' | 'faq' | 'location' | 'hub' | 'general';
  service?: Service;
  faqs?: FAQ[];
  locationName?: string;
  locationSlug?: string;     // NEW -- drives per-page areaServed
  pageTitle?: string;
  pageDescription?: string;
  pageUrl?: string;
}
```

Current hub page SchemaMarkup call (BROKEN -- passes invalid props):
```typescript
<SchemaMarkup
  type="service"
  serviceName="Roofing Services"
  serviceDescription="Professional roofing services..."
  areaServed={locations.map(loc => loc.name).join(', ')}
/>
```

Current subpage SchemaMarkup call pattern (missing locationSlug):
```typescript
<SchemaMarkup
  type="location"
  locationName="Gahanna"
  pageTitle="Roofers in Gahanna, OH | DTE Roofing"
  pageDescription="..."
  pageUrl="https://www.dteroofingllc.com/locations/gahanna"
/>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix hub page H1, meta title, and SchemaMarkup props</name>
  <files>src/pages/Locations.tsx</files>
  <read_first>
    - src/pages/Locations.tsx (current file -- see lines 91, 96-101, 111 for the 3 things to change)
    - src/components/SchemaMarkup.tsx (verify the 'hub' type is available after Plan 01)
  </read_first>
  <action>
Make exactly 3 changes to src/pages/Locations.tsx:

**Change 1 (HUB-02): SEO title prop (line 91)**
Change from:
```
title="Central Ohio Service Areas - DTE Roofing Locations | Columbus & Surrounding Cities"
```
To:
```
title="Areas We Serve in Central Ohio | DTE Roofing Service Areas"
```

**Change 2 (SCHEMA-01): SchemaMarkup invocation (lines 96-101)**
Replace the entire broken SchemaMarkup block:
```typescript
<SchemaMarkup
  type="service"
  serviceName="Roofing Services"
  serviceDescription="Professional roofing services throughout Central Ohio including Columbus, Hilliard, Dublin, and surrounding communities."
  areaServed={locations.map(loc => loc.name).join(', ')}
/>
```
With:
```typescript
<SchemaMarkup
  type="hub"
  locationName="Central Ohio"
  pageTitle="Areas We Serve in Central Ohio | DTE Roofing Service Areas"
  pageDescription="DTE Roofing serves 13 Central Ohio communities including Columbus, Hilliard, Dublin, New Albany, Westerville, Gahanna, and more. Find your local roofing experts."
  pageUrl="https://www.dteroofingllc.com/locations"
/>
```

**Change 3 (HUB-01): H1 text (line 111)**
Change from:
```
Roofing Contractor Columbus OH
```
To:
```
Areas We Serve in Central Ohio
```

Do NOT change any other content, layout, or styling in Locations.tsx. The `locations` array at the top of the file stays as-is (it's used for rendering cards, not schema).
  </action>
  <verify>
    <automated>cd "C:/Users/geoca/dte-roofing-web" && npx tsc --noEmit src/pages/Locations.tsx 2>&1 | head -10</automated>
  </verify>
  <acceptance_criteria>
    - Locations.tsx SEO title prop contains "Areas We Serve in Central Ohio | DTE Roofing Service Areas"
    - Locations.tsx SchemaMarkup uses type="hub" (not type="service")
    - Locations.tsx SchemaMarkup does NOT contain props `serviceName`, `serviceDescription`, or `areaServed`
    - Locations.tsx SchemaMarkup contains pageUrl="https://www.dteroofingllc.com/locations"
    - Locations.tsx H1 contains "Areas We Serve in Central Ohio" (not "Roofing Contractor Columbus OH")
    - Locations.tsx still contains the `locations` array with 13 entries (unchanged)
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>Hub page H1 reads "Areas We Serve in Central Ohio", meta title matches, and SchemaMarkup uses type="hub" with valid props that will generate RoofingContractor JSON-LD with all 13 cities in areaServed.</done>
</task>

<task type="auto">
  <name>Task 2: Add locationSlug prop to all 13 location subpages</name>
  <files>
    src/pages/locations/Columbus.tsx,
    src/pages/locations/Hilliard.tsx,
    src/pages/locations/Dublin.tsx,
    src/pages/locations/NewAlbany.tsx,
    src/pages/locations/UpperArlington.tsx,
    src/pages/locations/Westerville.tsx,
    src/pages/locations/Gahanna.tsx,
    src/pages/locations/Reynoldsburg.tsx,
    src/pages/locations/GroveCity.tsx,
    src/pages/locations/Pickerington.tsx,
    src/pages/locations/Worthington.tsx,
    src/pages/locations/Delaware.tsx,
    src/pages/locations/Powell.tsx
  </files>
  <read_first>
    - src/pages/locations/Columbus.tsx (see current SchemaMarkup usage pattern)
    - src/pages/locations/Gahanna.tsx (second example to confirm pattern is consistent)
    - src/pages/locations/NewAlbany.tsx (verify slug format for hyphenated city)
  </read_first>
  <action>
For each of the 13 location subpage files, add ONE prop to the existing SchemaMarkup component call: `locationSlug="<slug>"`.

The slug for each file (must match the slug in locations.ts):

| File | Add this prop |
|------|---------------|
| Columbus.tsx | `locationSlug="columbus"` |
| Hilliard.tsx | `locationSlug="hilliard"` |
| Dublin.tsx | `locationSlug="dublin"` |
| NewAlbany.tsx | `locationSlug="new-albany"` |
| UpperArlington.tsx | `locationSlug="upper-arlington"` |
| Westerville.tsx | `locationSlug="westerville"` |
| Gahanna.tsx | `locationSlug="gahanna"` |
| Reynoldsburg.tsx | `locationSlug="reynoldsburg"` |
| GroveCity.tsx | `locationSlug="grove-city"` |
| Pickerington.tsx | `locationSlug="pickerington"` |
| Worthington.tsx | `locationSlug="worthington"` |
| Delaware.tsx | `locationSlug="delaware"` |
| Powell.tsx | `locationSlug="powell"` |

Insert `locationSlug` as a new prop line right after the existing `type="location"` line in each file's SchemaMarkup JSX. Do NOT change any other props (locationName, pageTitle, pageDescription, pageUrl all stay exactly as they are). Do NOT change any content in these files.

Example for Columbus.tsx -- the SchemaMarkup block should look like:
```typescript
<SchemaMarkup
  type="location"
  locationSlug="columbus"
  locationName="Columbus"
  pageTitle="Roofers in Columbus, OH | DTE Roofing"
  pageDescription="..."
  pageUrl="https://www.dteroofingllc.com/locations/columbus"
/>
```
  </action>
  <verify>
    <automated>cd "C:/Users/geoca/dte-roofing-web" && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - All 13 files in src/pages/locations/ contain `locationSlug=` (grep returns 13 matches)
    - Columbus.tsx contains `locationSlug="columbus"`
    - NewAlbany.tsx contains `locationSlug="new-albany"`
    - UpperArlington.tsx contains `locationSlug="upper-arlington"`
    - GroveCity.tsx contains `locationSlug="grove-city"`
    - Each file's locationSlug value matches the slug in its pageUrl (e.g., pageUrl contains `/locations/columbus` and locationSlug is "columbus")
    - No other changes to locationName, pageTitle, pageDescription, or pageUrl in any file
    - Full project TypeScript compilation passes (npx tsc --noEmit exits 0)
  </acceptance_criteria>
  <done>All 13 location subpages pass their correct locationSlug to SchemaMarkup, enabling unique @id and page-specific areaServed generation for each page.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with zero errors
2. `grep -r "locationSlug=" src/pages/locations/ | wc -l` returns 13
3. `grep 'type="hub"' src/pages/Locations.tsx` returns 1 match
4. `grep "Roofing Contractor Columbus OH" src/pages/Locations.tsx` returns 0 matches
5. `grep "Areas We Serve in Central Ohio" src/pages/Locations.tsx` returns at least 2 matches (H1 + title)
6. `grep "serviceName\|serviceDescription" src/pages/Locations.tsx` returns 0 matches (broken props removed)
7. Dev server starts without errors: `npx vite build 2>&1 | tail -5` exits cleanly
</verification>

<success_criteria>
- Hub page H1 says "Areas We Serve in Central Ohio"
- Hub page meta title is "Areas We Serve in Central Ohio | DTE Roofing Service Areas"
- Hub page uses type="hub" SchemaMarkup producing RoofingContractor with all 13 cities
- All 13 subpages pass correct locationSlug matching their URL slug
- Full TypeScript compilation passes
- Vite build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/01-data-architecture-schema-fixes/01-02-SUMMARY.md`
</output>
