---
phase: 03-hub-enhancements-service-cross-links
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/services/RoofInstallation.tsx
  - src/pages/services/RoofInspection.tsx
  - src/pages/services/PreventativeMaintenance.tsx
  - src/pages/services/Siding.tsx
autonomous: false
requirements:
  - LINK-04
must_haves:
  truths:
    - "At least 9 of 13 cities have location links from 2+ service pages (up from current 4 cities)"
    - "No existing page content text is modified -- only plain-text city names are wrapped in Link elements"
    - "All new links use the exact same className as existing location links on those pages"
  artifacts:
    - path: "src/pages/services/RoofInstallation.tsx"
      provides: "Additional location links to Grove City"
      contains: "/locations/grove-city"
    - path: "src/pages/services/RoofInspection.tsx"
      provides: "Additional location link to Grove City"
      contains: "/locations/grove-city"
    - path: "src/pages/services/PreventativeMaintenance.tsx"
      provides: "Additional location link to Grove City"
      contains: "/locations/grove-city"
    - path: "src/pages/services/Siding.tsx"
      provides: "Additional location links to Grove City"
      contains: "/locations/grove-city"
  key_links:
    - from: "src/pages/services/RoofInstallation.tsx"
      to: "/locations/grove-city"
      via: "inline Link component"
      pattern: 'to="/locations/grove-city"'
    - from: "src/pages/services/Siding.tsx"
      to: "/locations/grove-city"
      via: "inline Link component"
      pattern: 'to="/locations/grove-city"'
---

<objective>
Improve service-to-location cross-link distribution by converting plain-text city name mentions to clickable Link elements on service pages, focusing on underserved cities that currently only appear as links on 1 service page.

Purpose: Satisfy LINK-04 -- ensure service pages link naturally to relevant location pages. All 12 pages already have 2-3+ links, but 9 of 13 cities only appear on 1 page. Converting existing plain-text mentions to links improves distribution without adding new content.
Output: Updated service page files with additional location links.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/milestones/v1.0-phases/03-hub-enhancements-service-cross-links/03-RESEARCH.md
@.planning/milestones/v1.0-phases/03-hub-enhancements-service-cross-links/03-UI-SPEC.md

<interfaces>
<!-- Existing link pattern used on ALL service pages (MUST match exactly) -->

```typescript
import { Link } from 'react-router-dom';

// Exact className for inline location links:
<Link
  to="/locations/{slug}"
  className="text-primary-700 hover:text-primary-800 font-semibold underline decoration-2 underline-offset-2"
>
  {anchor text -- same visible text as before}
</Link>
```

<!-- Current location link distribution across service pages -->
<!-- Cities with only 1 service page linking to them: -->
<!-- worthington (GutterServices only), westerville (RoofMaintenance only), -->
<!-- upper-arlington (EmergencyServices only), reynoldsburg (EmergencyServices only), -->
<!-- powell (RoofMaintenance only), pickerington (EmergencyServices only), -->
<!-- new-albany (GutterServices only), gahanna (RoofMaintenance only), -->
<!-- delaware (GutterServices only) -->
<!-- grove-city: 3 pages (CommercialRoofing, RoofReplacement, StormDamage) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert plain-text city names to location links on 4 service pages</name>
  <files>
    src/pages/services/RoofInstallation.tsx
    src/pages/services/RoofInspection.tsx
    src/pages/services/PreventativeMaintenance.tsx
    src/pages/services/Siding.tsx
  </files>
  <read_first>
    - src/pages/services/RoofInstallation.tsx
    - src/pages/services/RoofInspection.tsx
    - src/pages/services/PreventativeMaintenance.tsx
    - src/pages/services/Siding.tsx
    - .planning/milestones/v1.0-phases/03-hub-enhancements-service-cross-links/03-UI-SPEC.md
  </read_first>
  <action>
Convert specific plain-text city name mentions to `&lt;Link&gt;` elements. The visible text MUST remain identical -- only wrap it in a Link tag. Do NOT change any surrounding text, do NOT add new sentences, do NOT rearrange content.

**Required link className (exact):**
`"text-primary-700 hover:text-primary-800 font-semibold underline decoration-2 underline-offset-2"`

**Ensure `Link` is imported** from `react-router-dom` at the top of each file. (All 4 files already import Link -- verify before adding a duplicate import.)

**File: RoofInstallation.tsx**
Convert these 2 plain-text mentions to links:
1. Line ~205: `"Budget-conscious homeowners in Grove City and Columbus"` -- wrap `Grove City` in `&lt;Link to="/locations/grove-city"&gt;Grove City&lt;/Link&gt;`
2. Line ~479: `"penetrations common in older Grove City and Hilliard homes"` -- wrap `Grove City` in `&lt;Link to="/locations/grove-city"&gt;Grove City&lt;/Link&gt;`

**File: RoofInspection.tsx**
Convert this 1 plain-text mention to a link:
1. Line ~33: `"and Grove City, DTE Roofing provides"` -- wrap `Grove City` in `&lt;Link to="/locations/grove-city"&gt;Grove City&lt;/Link&gt;`

**File: PreventativeMaintenance.tsx**
Convert this 1 plain-text mention to a link:
1. Line ~34: find "Grove City" in the opening paragraph (same line as existing Columbus/Hilliard/Dublin links) -- wrap `Grove City` in `&lt;Link to="/locations/grove-city"&gt;Grove City&lt;/Link&gt;`

**File: Siding.tsx**
Convert these 3 plain-text mentions to links (pick only 1 per paragraph to avoid over-linking):
1. Line ~260: `"Homeowners in Grove City and Columbus"` -- wrap `Grove City` in `&lt;Link to="/locations/grove-city"&gt;Grove City&lt;/Link&gt;`
2. Line ~373: `"Wood and engineered wood siding in Grove City and Columbus"` -- wrap `Grove City` in `&lt;Link to="/locations/grove-city"&gt;Grove City&lt;/Link&gt;`
3. Line ~461: `"Simple ranch-style homes in Grove City"` -- wrap `Grove City` in `&lt;Link to="/locations/grove-city"&gt;Grove City&lt;/Link&gt;`

**CRITICAL CONSTRAINTS:**
- Do NOT modify any text content -- only wrap existing city names in Link tags
- Do NOT add any new paragraphs, sentences, or sections
- Do NOT convert Columbus/Hilliard/Dublin mentions (they already have sufficient links)
- Verify each file still compiles after changes
  </action>
  <verify>
    <automated>cd C:/Users/geoca/dte-roofing-web && npx tsc --noEmit src/pages/services/RoofInstallation.tsx src/pages/services/RoofInspection.tsx src/pages/services/PreventativeMaintenance.tsx src/pages/services/Siding.tsx 2>&1 | head -20 && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - RoofInstallation.tsx contains `to="/locations/grove-city"` (at least 2 occurrences)
    - RoofInspection.tsx contains `to="/locations/grove-city"` (at least 1 occurrence)
    - PreventativeMaintenance.tsx contains `to="/locations/grove-city"` (at least 1 occurrence)
    - Siding.tsx contains `to="/locations/grove-city"` (at least 3 occurrences)
    - All new Link elements use className containing `text-primary-700 hover:text-primary-800 font-semibold underline decoration-2 underline-offset-2`
    - No existing paragraph text is changed (only Link wrappers added around existing city names)
    - TypeScript compiles without errors for all 4 files
    - Build succeeds (npm run build exits 0)
    - Running `grep -c '/locations/' src/pages/services/RoofInstallation.tsx` shows at least 5 (was 3)
    - Running `grep -c '/locations/' src/pages/services/Siding.tsx` shows at least 4 (was 3)
  </acceptance_criteria>
  <done>Plain-text "Grove City" mentions on 4 service pages converted to Link elements. Grove City now linked from 7 service pages (was 3). All links use consistent styling. No content text modified. Build passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify cross-link additions look correct</name>
  <files>
    src/pages/services/RoofInstallation.tsx
    src/pages/services/RoofInspection.tsx
    src/pages/services/PreventativeMaintenance.tsx
    src/pages/services/Siding.tsx
  </files>
  <action>
Human visual verification of the newly added location links. No automated action -- this checkpoint confirms the links look visually correct and match existing link styling on each page.
  </action>
  <verify>User confirms links look correct by typing "approved".</verify>
  <done>User has visually confirmed that all new Grove City links on the 4 service pages match existing link styling and navigate correctly to /locations/grove-city.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` -- zero TypeScript errors across all modified files
2. `npm run build` -- production build succeeds
3. `grep -c '/locations/' src/pages/services/RoofInstallation.tsx` returns 5+ (was 3)
4. `grep -c '/locations/' src/pages/services/RoofInspection.tsx` returns 4+ (was 3)
5. `grep -c '/locations/' src/pages/services/PreventativeMaintenance.tsx` returns 4+ (was 3)
6. `grep -c '/locations/' src/pages/services/Siding.tsx` returns 4+ (was 3)
7. Visual inspection confirms links look identical to existing location links on same pages
</verification>

<success_criteria>
- Grove City linked from at least 7 service pages (was 3)
- All new links use exact same className as existing location links
- No content text modified (diff shows only Link wrapper additions)
- All 12 service pages still have 2-3+ location links
- Build passes
</success_criteria>

<output>
After completion, create `.planning/milestones/v1.0-phases/03-hub-enhancements-service-cross-links/03-02-SUMMARY.md`
</output>
