---
phase: 03-hub-enhancements-service-cross-links
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ServiceAreaMap.tsx
  - src/pages/Locations.tsx
autonomous: true
requirements:
  - HUB-03
must_haves:
  truths:
    - "The /locations hub page displays a visual SVG map showing 13 Central Ohio city positions"
    - "Each city dot on the map links to its respective /locations/{slug} page"
    - "The map is accessible with role=img, aria-label, and title element"
  artifacts:
    - path: "src/components/ServiceAreaMap.tsx"
      provides: "SVG-based service area map component"
      min_lines: 50
      exports: ["default"]
    - path: "src/pages/Locations.tsx"
      provides: "Hub page with map section inserted between hero and city grid"
      contains: "ServiceAreaMap"
  key_links:
    - from: "src/components/ServiceAreaMap.tsx"
      to: "src/data/locations.ts"
      via: "import LOCATIONS"
      pattern: "import.*LOCATIONS.*from.*locations"
    - from: "src/pages/Locations.tsx"
      to: "src/components/ServiceAreaMap.tsx"
      via: "import and render"
      pattern: "import ServiceAreaMap"
---

<objective>
Create an SVG-based service area map component and add it to the /locations hub page between the hero section and city grid.

Purpose: Satisfy HUB-03 -- provide visual geographic context for DTE Roofing's 13 Central Ohio service areas before users browse individual city cards.
Output: ServiceAreaMap.tsx component, updated Locations.tsx with map section.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/milestones/v1.0-phases/03-hub-enhancements-service-cross-links/03-RESEARCH.md
@.planning/milestones/v1.0-phases/03-hub-enhancements-service-cross-links/03-UI-SPEC.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/data/locations.ts:
```typescript
export interface LocationConfig {
  slug: string;
  cityName: string;
  stateAbbr: string;
  neighbors: string[];
}

export const LOCATIONS: LocationConfig[] = [
  { slug: 'columbus', cityName: 'Columbus', stateAbbr: 'OH', neighbors: [...] },
  // ... 13 total cities
];
```

From src/pages/Locations.tsx (insertion point):
```typescript
// Line 126: </section> (end of hero section)
// Line 127: (BLANK)
// Line 128: <section className="py-20 bg-gray-50"> (start of city grid section)
// INSERT NEW MAP SECTION between line 126 and line 128
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ServiceAreaMap SVG component</name>
  <files>src/components/ServiceAreaMap.tsx</files>
  <read_first>
    - src/data/locations.ts
    - .planning/milestones/v1.0-phases/03-hub-enhancements-service-cross-links/03-UI-SPEC.md
    - .planning/milestones/v1.0-phases/03-hub-enhancements-service-cross-links/03-RESEARCH.md
  </read_first>
  <action>
Create new file `src/components/ServiceAreaMap.tsx` with a default export function `ServiceAreaMap` that returns JSX.Element.

**Imports:**
- `Link` from `react-router-dom`
- `LOCATIONS` from `../data/locations`

**CITY_POSITIONS constant** (Record&lt;string, { x: number; y: number }&gt;) with these exact values:
```
'delaware':        { x: 50, y: 10 }
'powell':          { x: 45, y: 25 }
'westerville':     { x: 60, y: 30 }
'dublin':          { x: 30, y: 35 }
'worthington':     { x: 50, y: 35 }
'new-albany':      { x: 75, y: 40 }
'upper-arlington': { x: 35, y: 45 }
'gahanna':         { x: 65, y: 45 }
'columbus':        { x: 50, y: 50 }
'hilliard':        { x: 25, y: 50 }
'reynoldsburg':    { x: 70, y: 55 }
'grove-city':      { x: 35, y: 65 }
'pickerington':    { x: 70, y: 65 }
```

**Component structure:**
1. Outer `div` with `className="bg-white rounded-xl border-2 border-gray-200 p-6"`
2. `svg` element with:
   - `viewBox="0 0 100 80"`
   - `role="img"`
   - `aria-label="Map of DTE Roofing service areas across 13 Central Ohio cities"`
   - `className="w-full max-w-2xl mx-auto"`
3. First SVG child: `<title>DTE Roofing Service Area Map</title>`
4. Second SVG child: `<ellipse cx="50" cy="40" rx="35" ry="30" className="fill-primary-50 stroke-primary-200" strokeWidth="0.5" />`
5. Map over `LOCATIONS` array. For each location:
   - Look up position from `CITY_POSITIONS[loc.slug]`
   - If no position found, return null
   - Wrap in `<Link key={loc.slug} to={/locations/${loc.slug}}>`
   - Render `<circle cx={pos.x} cy={pos.y} r="1.5" className="fill-primary-700 hover:fill-primary-500 cursor-pointer transition-colors" />`
   - Render `<text x={pos.x} y={pos.y + 4} textAnchor="middle" className="fill-charcoal-700 text-[2.5px] font-semibold">{loc.cityName}</text>`

**Return type annotation:** `JSX.Element` on the function signature.
  </action>
  <verify>
    <automated>cd C:/Users/geoca/dte-roofing-web && npx tsc --noEmit src/components/ServiceAreaMap.tsx 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - File exists at src/components/ServiceAreaMap.tsx
    - File contains `export default function ServiceAreaMap()`
    - File contains `import { LOCATIONS } from '../data/locations'`
    - File contains `import { Link } from 'react-router-dom'`
    - File contains `role="img"`
    - File contains `aria-label="Map of DTE Roofing service areas across 13 Central Ohio cities"`
    - File contains `<title>DTE Roofing Service Area Map</title>`
    - File contains `viewBox="0 0 100 80"`
    - File contains `CITY_POSITIONS` with all 13 city slugs as keys
    - File contains `fill-primary-700 hover:fill-primary-500`
    - File contains `fill-charcoal-700 text-[2.5px] font-semibold`
    - File contains `fill-primary-50 stroke-primary-200`
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>ServiceAreaMap.tsx exists, exports default function, renders SVG with 13 city dots linked to location pages, accessible with role/aria-label/title, uses LOCATIONS from data source, TypeScript compiles clean.</done>
</task>

<task type="auto">
  <name>Task 2: Insert ServiceAreaMap section into Locations hub page</name>
  <files>src/pages/Locations.tsx</files>
  <read_first>
    - src/pages/Locations.tsx
    - src/components/ServiceAreaMap.tsx
    - .planning/milestones/v1.0-phases/03-hub-enhancements-service-cross-links/03-UI-SPEC.md
  </read_first>
  <action>
Edit `src/pages/Locations.tsx` to add the ServiceAreaMap section between the hero section and the city grid section.

**Add import** at top of file (after existing imports):
```typescript
import ServiceAreaMap from '../components/ServiceAreaMap';
```

**Insert new section** between the closing `</section>` of the hero (line ~126) and the opening `<section className="py-20 bg-gray-50">` of the city grid (line ~128). The new section JSX:

```tsx
<section className="py-12 bg-white">
  <div className="container mx-auto px-4">
    <div className="max-w-3xl mx-auto text-center mb-8">
      <h2 className="text-2xl font-bold text-charcoal-900 mb-2">Our Service Area</h2>
      <p className="text-charcoal-600">Serving 13 communities across Central Ohio</p>
    </div>
    <ServiceAreaMap />
  </div>
</section>
```

**Do NOT** modify any other section of Locations.tsx. The hero, city grid, stats, info cards, and CTA sections must remain unchanged.
  </action>
  <verify>
    <automated>cd C:/Users/geoca/dte-roofing-web && npx tsc --noEmit src/pages/Locations.tsx 2>&1 | head -20 && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/pages/Locations.tsx contains `import ServiceAreaMap from '../components/ServiceAreaMap'`
    - src/pages/Locations.tsx contains `<ServiceAreaMap />`
    - src/pages/Locations.tsx contains `Our Service Area`
    - src/pages/Locations.tsx contains `Serving 13 communities across Central Ohio`
    - src/pages/Locations.tsx contains `py-12 bg-white` (map section wrapper)
    - The map section appears AFTER the hero section closing tag and BEFORE the city grid section
    - TypeScript compiles without errors
    - Build succeeds (npm run build exits 0)
  </acceptance_criteria>
  <done>Locations.tsx imports and renders ServiceAreaMap in a new section between hero and city grid. Section has heading "Our Service Area" and subheading. Build passes.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` -- zero TypeScript errors
2. `npm run build` -- production build succeeds
3. Visual check: `npm run dev` and visit /locations -- map section visible between hero and city grid with 13 clickable city dots
4. Each city dot links to correct /locations/{slug} URL
5. SVG has role="img" and aria-label attributes
</verification>

<success_criteria>
- ServiceAreaMap.tsx exists and exports a default function component
- Locations.tsx renders ServiceAreaMap between hero and city grid sections
- All 13 cities from LOCATIONS array rendered as clickable dots with labels
- SVG is accessible (role, aria-label, title)
- No new dependencies added
- Build passes
</success_criteria>

<output>
After completion, create `.planning/milestones/v1.0-phases/03-hub-enhancements-service-cross-links/03-01-SUMMARY.md`
</output>
