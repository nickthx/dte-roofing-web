---
phase: 02-internal-linking-navigation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/Footer.tsx
autonomous: true
requirements:
  - LINK-03

must_haves:
  truths:
    - "The site footer lists all 13 location pages as clickable links"
    - "The footer contains an 'All Service Areas' link pointing to /locations"
    - "The footer is visible on every page of the site"
  artifacts:
    - path: "src/components/Footer.tsx"
      provides: "Full 13-city footer with dynamic LOCATIONS import"
      contains: "LOCATIONS"
  key_links:
    - from: "src/components/Footer.tsx"
      to: "src/data/locations.ts"
      via: "LOCATIONS array import"
      pattern: "import.*LOCATIONS.*from.*locations"
---

<objective>
Update the footer to display all 13 location pages instead of the current hardcoded 5.

Purpose: Ensures all 13 service area pages are discoverable from every page on the site, completing the footer coverage requirement for local SEO.
Output: Updated Footer.tsx with dynamic city list from LOCATIONS array in a 2-column sub-grid layout.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-internal-linking-navigation/02-UI-SPEC.md

@src/components/Footer.tsx
@src/data/locations.ts
</context>

<interfaces>
<!-- Current Footer.tsx "Areas We Serve" section (lines 46-56) to be replaced: -->
```typescript
<div>
  <h4 className="font-bold text-lg mb-4">Areas We Serve</h4>
  <ul className="space-y-2 text-gray-400">
    <li><Link to="/locations/hilliard" className="hover:text-white transition-colors">Hilliard</Link></li>
    <li><Link to="/locations/dublin" className="hover:text-white transition-colors">Dublin</Link></li>
    <li><Link to="/locations/columbus" className="hover:text-white transition-colors">Columbus</Link></li>
    <li><Link to="/locations/westerville" className="hover:text-white transition-colors">Westerville</Link></li>
    <li><Link to="/locations/powell" className="hover:text-white transition-colors">Powell</Link></li>
    <li><Link to="/locations" className="hover:text-white transition-colors">All Service Areas &rarr;</Link></li>
  </ul>
</div>
```

<!-- LOCATIONS array from src/data/locations.ts (13 entries): -->
```typescript
export const LOCATIONS: LocationConfig[] = [
  { slug: 'columbus', cityName: 'Columbus', ... },
  { slug: 'hilliard', cityName: 'Hilliard', ... },
  { slug: 'dublin', cityName: 'Dublin', ... },
  { slug: 'new-albany', cityName: 'New Albany', ... },
  { slug: 'upper-arlington', cityName: 'Upper Arlington', ... },
  { slug: 'westerville', cityName: 'Westerville', ... },
  { slug: 'gahanna', cityName: 'Gahanna', ... },
  { slug: 'reynoldsburg', cityName: 'Reynoldsburg', ... },
  { slug: 'grove-city', cityName: 'Grove City', ... },
  { slug: 'pickerington', cityName: 'Pickerington', ... },
  { slug: 'worthington', cityName: 'Worthington', ... },
  { slug: 'delaware', cityName: 'Delaware', ... },
  { slug: 'powell', cityName: 'Powell', ... },
];
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Replace hardcoded 5-city footer list with dynamic 13-city list from LOCATIONS</name>
  <files>src/components/Footer.tsx</files>
  <read_first>
    - src/components/Footer.tsx (current footer implementation, especially lines 46-56)
    - src/data/locations.ts (LOCATIONS array and LocationConfig type)
    - .planning/phases/02-internal-linking-navigation/02-UI-SPEC.md (footer styling specs)
  </read_first>
  <action>
1. **Add import** at the top of Footer.tsx (after existing imports):
   ```
   import { LOCATIONS } from '../data/locations';
   ```

2. **Replace the "Areas We Serve" column** (the `<div>` containing `<h4>Areas We Serve</h4>` and the `<ul>` with 5 hardcoded cities, approximately lines 46-56) with:

   ```tsx
   <div>
     <h4 className="font-bold text-lg mb-4">Areas We Serve</h4>
     <div className="grid grid-cols-2 gap-x-4 gap-y-2 text-gray-400 text-sm">
       {LOCATIONS.map((loc) => (
         <Link
           key={loc.slug}
           to={`/locations/${loc.slug}`}
           className="hover:text-white transition-colors"
         >
           {loc.cityName}
         </Link>
       ))}
     </div>
     <Link
       to="/locations"
       className="text-gray-400 hover:text-white transition-colors text-sm font-bold mt-4 inline-block"
     >
       All Service Areas &rarr;
     </Link>
   </div>
   ```

Key details from UI-SPEC:
- Layout changes from `<ul className="space-y-2">` to `<div className="grid grid-cols-2 gap-x-4 gap-y-2">`
- Link text size: `text-sm` (14px)
- Link color: `text-gray-400 hover:text-white transition-colors`
- "All Service Areas" link: separate element below grid, `text-sm font-bold mt-4 inline-block`
- Arrow: HTML entity `&rarr;`
- The `<h4>` heading remains unchanged: `className="font-bold text-lg mb-4"`, text "Areas We Serve"

Do NOT modify any other footer columns (Services, Company, Contact), the bottom credentials bar, or the copyright line.
  </action>
  <verify>
    <automated>cd C:/Users/geoca/dte-roofing-web && npx tsc --noEmit src/components/Footer.tsx 2>&1 | tail -5 && echo "---" && grep -c "LOCATIONS" src/components/Footer.tsx && echo "LOCATIONS refs" && grep "grid-cols-2" src/components/Footer.tsx | head -1</automated>
  </verify>
  <acceptance_criteria>
    - File `src/components/Footer.tsx` contains `import { LOCATIONS } from '../data/locations'`
    - File `src/components/Footer.tsx` contains `grid grid-cols-2 gap-x-4 gap-y-2`
    - File `src/components/Footer.tsx` contains `LOCATIONS.map`
    - File `src/components/Footer.tsx` contains `loc.cityName`
    - File `src/components/Footer.tsx` contains `All Service Areas`
    - File `src/components/Footer.tsx` contains `to="/locations"`
    - File `src/components/Footer.tsx` contains `font-bold mt-4 inline-block`
    - File `src/components/Footer.tsx` does NOT contain `to="/locations/hilliard"` as a hardcoded string (old hardcoded list removed)
    - File `src/components/Footer.tsx` does NOT contain `to="/locations/dublin"` as a hardcoded string
    - TypeScript compilation passes with no errors
    - The rest of the footer is unchanged: file still contains "Licensed & Insured", "GAF Certified", "BBB Accredited"
  </acceptance_criteria>
  <done>Footer displays all 13 cities from LOCATIONS array in a 2-column sub-grid with "All Service Areas" link, replacing the old hardcoded 5-city list. Full type-check passes.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with zero errors
2. `grep "LOCATIONS" src/components/Footer.tsx` shows import and .map usage
3. `npm run build` completes successfully
4. No hardcoded city links remain in the "Areas We Serve" section
</verification>

<success_criteria>
- Footer "Areas We Serve" column dynamically renders all 13 cities from LOCATIONS array
- 2-column sub-grid layout keeps footer height balanced
- "All Service Areas" link to /locations present below city grid
- No other footer sections modified
- Full TypeScript and build pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-internal-linking-navigation/02-02-SUMMARY.md`
</output>
