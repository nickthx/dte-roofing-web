---
phase: 02-internal-linking-navigation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/LocationBreadcrumb.tsx
  - src/components/NearbyAreas.tsx
  - src/pages/locations/Columbus.tsx
  - src/pages/locations/Hilliard.tsx
  - src/pages/locations/Dublin.tsx
  - src/pages/locations/NewAlbany.tsx
  - src/pages/locations/UpperArlington.tsx
  - src/pages/locations/Westerville.tsx
  - src/pages/locations/Gahanna.tsx
  - src/pages/locations/Reynoldsburg.tsx
  - src/pages/locations/GroveCity.tsx
  - src/pages/locations/Pickerington.tsx
  - src/pages/locations/Worthington.tsx
  - src/pages/locations/Delaware.tsx
  - src/pages/locations/Powell.tsx
autonomous: true
requirements:
  - LINK-01
  - LINK-02

must_haves:
  truths:
    - "Every location subpage displays a breadcrumb bar below the hero with 'All Service Areas > {City Name}' linking to /locations"
    - "Every location subpage displays a 'Nearby Areas We Serve' section with 3-5 linked cards to neighbor location pages"
    - "Clicking a neighbor card navigates to that location's page via client-side routing"
  artifacts:
    - path: "src/components/LocationBreadcrumb.tsx"
      provides: "Breadcrumb navigation component"
      exports: ["default"]
    - path: "src/components/NearbyAreas.tsx"
      provides: "Nearby areas cross-link component"
      exports: ["default"]
  key_links:
    - from: "src/components/NearbyAreas.tsx"
      to: "src/data/locations.ts"
      via: "getLocationBySlug import"
      pattern: "getLocationBySlug"
    - from: "src/pages/locations/*.tsx"
      to: "src/components/LocationBreadcrumb.tsx"
      via: "import and render"
      pattern: "LocationBreadcrumb"
    - from: "src/pages/locations/*.tsx"
      to: "src/components/NearbyAreas.tsx"
      via: "import and render"
      pattern: "NearbyAreas"
---

<objective>
Create LocationBreadcrumb and NearbyAreas components and wire them into all 13 location subpages.

Purpose: Establishes visible navigation hierarchy (breadcrumb to hub) and internal cross-linking mesh (neighbor links) for SEO authority and user navigation.
Output: Two new reusable components, 13 location pages updated with breadcrumb + nearby areas sections.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-internal-linking-navigation/02-UI-SPEC.md

@src/data/locations.ts
@src/pages/locations/Columbus.tsx
</context>

<interfaces>
<!-- Key types and contracts the executor needs from src/data/locations.ts -->

```typescript
export interface LocationConfig {
  slug: string;
  cityName: string;
  stateAbbr: string;
  neighbors: string[];
}

export const LOCATIONS: LocationConfig[] = [ /* 13 entries */ ];

export const getLocationBySlug = (slug: string): LocationConfig | undefined =>
  LOCATIONS.find((loc) => loc.slug === slug);
```

<!-- Location page structure pattern (all 13 pages follow this): -->
```
<div className="min-h-screen bg-white">
  <SEO ... />
  <SchemaMarkup ... />
  <section className="relative bg-gradient-to-br from-charcoal-900 ..."> <!-- HERO --> </section>
  <!-- INSERT LocationBreadcrumb HERE (sibling between hero and content) -->
  <section className="py-20 bg-white">
    <div className="container mx-auto px-4">
      <!-- ... existing content blocks ... -->
      <!-- INSERT NearbyAreas HERE (before the gradient CTA div) -->
      <div className="bg-gradient-to-br from-primary-700 to-primary-800 ..."> <!-- inline CTA --> </div>
    </div>
  </section>
  <section className="py-20 bg-gray-50"> <!-- bottom CTA --> </section>
</div>
```

<!-- Each page's cityName and slug for the component props: -->
| File | cityName prop | locationSlug prop |
|------|---------------|-------------------|
| Columbus.tsx | "Columbus" | "columbus" |
| Hilliard.tsx | "Hilliard" | "hilliard" |
| Dublin.tsx | "Dublin" | "dublin" |
| NewAlbany.tsx | "New Albany" | "new-albany" |
| UpperArlington.tsx | "Upper Arlington" | "upper-arlington" |
| Westerville.tsx | "Westerville" | "westerville" |
| Gahanna.tsx | "Gahanna" | "gahanna" |
| Reynoldsburg.tsx | "Reynoldsburg" | "reynoldsburg" |
| GroveCity.tsx | "Grove City" | "grove-city" |
| Pickerington.tsx | "Pickerington" | "pickerington" |
| Worthington.tsx | "Worthington" | "worthington" |
| Delaware.tsx | "Delaware" | "delaware" |
| Powell.tsx | "Powell" | "powell" |
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create LocationBreadcrumb and NearbyAreas components</name>
  <files>src/components/LocationBreadcrumb.tsx, src/components/NearbyAreas.tsx</files>
  <read_first>
    - src/data/locations.ts (source of truth for LocationConfig type and getLocationBySlug)
    - .planning/phases/02-internal-linking-navigation/02-UI-SPEC.md (exact styling specs)
  </read_first>
  <action>
Create `src/components/LocationBreadcrumb.tsx`:
- Interface: `LocationBreadcrumbProps { cityName: string }`
- Export: `export default function LocationBreadcrumb({ cityName }: LocationBreadcrumbProps): JSX.Element`
- Outer div: `className="bg-gray-50 border-b border-gray-200"`
- Inner wrapper: `className="container mx-auto px-4 py-2"`
- Nav element: `<nav aria-label="Breadcrumb" className="flex items-center gap-2 text-sm">`
- Link (react-router-dom): `to="/locations"`, `className="text-primary-700 hover:text-primary-800 font-bold transition-colors"`, text: "All Service Areas"
- ChevronRight icon (lucide-react): `className="w-4 h-4 text-gray-400"`
- Current page span: `className="text-charcoal-700 font-normal"`, content: `{cityName}`
- Imports: `Link` from react-router-dom, `ChevronRight` from lucide-react

Create `src/components/NearbyAreas.tsx`:
- Interface: `NearbyAreasProps { locationSlug: string }`
- Export: `export default function NearbyAreas({ locationSlug }: NearbyAreasProps): JSX.Element | null`
- Import `getLocationBySlug`, `LOCATIONS` from `../data/locations`, and type `LocationConfig`
- Import `Link` from react-router-dom, `MapPin` and `ArrowRight` from lucide-react
- Get location via `getLocationBySlug(locationSlug)`, return null if not found
- Resolve neighbors: `location.neighbors.slice(0, 5).map(slug => LOCATIONS.find(loc => loc.slug === slug)).filter((loc): loc is LocationConfig => loc !== undefined)`
- Outer div: `className="mb-16"`
- Heading: `<h2 className="text-3xl font-bold text-charcoal-900 mb-8 text-center">Nearby Areas We Serve</h2>`
- Grid: `className="grid sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-5 gap-4"`
- Each neighbor card is a `<Link>`:
  - `to={`/locations/${neighbor.slug}`}`
  - `className="bg-gray-50 p-6 rounded-xl border border-gray-200 hover:border-primary-700 hover:bg-primary-50 transition-all group text-center"`
  - MapPin icon: `className="w-6 h-6 text-primary-700 mx-auto mb-2"`
  - City name h3: `className="font-bold text-charcoal-900 group-hover:text-primary-700 transition-colors"`
  - Sub-link span: `className="text-sm text-primary-700 mt-2 inline-flex items-center"`, text: "View Services", with ArrowRight `className="w-3 h-3 ml-1"`
  </action>
  <verify>
    <automated>cd C:/Users/geoca/dte-roofing-web && npx tsc --noEmit src/components/LocationBreadcrumb.tsx src/components/NearbyAreas.tsx 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - File `src/components/LocationBreadcrumb.tsx` exists and contains `export default function LocationBreadcrumb`
    - File `src/components/LocationBreadcrumb.tsx` contains `aria-label="Breadcrumb"`
    - File `src/components/LocationBreadcrumb.tsx` contains `to="/locations"`
    - File `src/components/LocationBreadcrumb.tsx` contains `text-primary-700 hover:text-primary-800 font-bold`
    - File `src/components/LocationBreadcrumb.tsx` contains `py-2`
    - File `src/components/LocationBreadcrumb.tsx` contains `font-normal` (current page text weight)
    - File `src/components/NearbyAreas.tsx` exists and contains `export default function NearbyAreas`
    - File `src/components/NearbyAreas.tsx` contains `getLocationBySlug`
    - File `src/components/NearbyAreas.tsx` contains `.slice(0, 5)`
    - File `src/components/NearbyAreas.tsx` contains `Nearby Areas We Serve`
    - File `src/components/NearbyAreas.tsx` contains `mb-2` (MapPin bottom margin per UI-SPEC)
    - File `src/components/NearbyAreas.tsx` contains `View Services`
    - File `src/components/NearbyAreas.tsx` contains `lg:grid-cols-5`
    - TypeScript compilation passes with no errors
  </acceptance_criteria>
  <done>Both components exist, type-check cleanly, and match the UI-SPEC styling contract exactly.</done>
</task>

<task type="auto">
  <name>Task 2: Wire LocationBreadcrumb and NearbyAreas into all 13 location pages</name>
  <files>
    src/pages/locations/Columbus.tsx,
    src/pages/locations/Hilliard.tsx,
    src/pages/locations/Dublin.tsx,
    src/pages/locations/NewAlbany.tsx,
    src/pages/locations/UpperArlington.tsx,
    src/pages/locations/Westerville.tsx,
    src/pages/locations/Gahanna.tsx,
    src/pages/locations/Reynoldsburg.tsx,
    src/pages/locations/GroveCity.tsx,
    src/pages/locations/Pickerington.tsx,
    src/pages/locations/Worthington.tsx,
    src/pages/locations/Delaware.tsx,
    src/pages/locations/Powell.tsx
  </files>
  <read_first>
    - src/pages/locations/Columbus.tsx (reference pattern for insertion points)
    - src/pages/locations/Delaware.tsx (confirm same structure)
    - src/components/LocationBreadcrumb.tsx (just created in Task 1)
    - src/components/NearbyAreas.tsx (just created in Task 1)
  </read_first>
  <action>
For each of the 13 location page files, make these edits:

1. **Add imports** at the top of the file (after existing imports):
   ```
   import LocationBreadcrumb from '../../components/LocationBreadcrumb';
   import NearbyAreas from '../../components/NearbyAreas';
   ```

2. **Insert LocationBreadcrumb** as a sibling element immediately after the hero section closing `</section>` tag (the section with `bg-gradient-to-br from-charcoal-900`) and before the main content section (`<section className="py-20 bg-white">`):
   ```
   <LocationBreadcrumb cityName="{CITY_NAME}" />
   ```

3. **Insert NearbyAreas** inside the main content section's `<div className="container mx-auto px-4">`, immediately BEFORE the inline CTA div (`<div className="bg-gradient-to-br from-primary-700 to-primary-800"`):
   ```
   <NearbyAreas locationSlug="{SLUG}" />
   ```

Props per page (cityName / locationSlug):
- Columbus.tsx: "Columbus" / "columbus"
- Hilliard.tsx: "Hilliard" / "hilliard"
- Dublin.tsx: "Dublin" / "dublin"
- NewAlbany.tsx: "New Albany" / "new-albany"
- UpperArlington.tsx: "Upper Arlington" / "upper-arlington"
- Westerville.tsx: "Westerville" / "westerville"
- Gahanna.tsx: "Gahanna" / "gahanna"
- Reynoldsburg.tsx: "Reynoldsburg" / "reynoldsburg"
- GroveCity.tsx: "Grove City" / "grove-city"
- Pickerington.tsx: "Pickerington" / "pickerington"
- Worthington.tsx: "Worthington" / "worthington"
- Delaware.tsx: "Delaware" / "delaware"
- Powell.tsx: "Powell" / "powell"

Do NOT modify any existing content, headings, testimonials, FAQs, or service descriptions. Only add imports and the two component insertions per file.
  </action>
  <verify>
    <automated>cd C:/Users/geoca/dte-roofing-web && npx tsc --noEmit 2>&1 | tail -5 && echo "---" && grep -l "LocationBreadcrumb" src/pages/locations/*.tsx | wc -l && echo "files have breadcrumb" && grep -l "NearbyAreas" src/pages/locations/*.tsx | wc -l && echo "files have nearby"</automated>
  </verify>
  <acceptance_criteria>
    - TypeScript compilation passes with no errors across the entire project
    - All 13 files in src/pages/locations/ contain the string `LocationBreadcrumb`
    - All 13 files in src/pages/locations/ contain the string `NearbyAreas`
    - All 13 files contain `import LocationBreadcrumb from '../../components/LocationBreadcrumb'`
    - All 13 files contain `import NearbyAreas from '../../components/NearbyAreas'`
    - Columbus.tsx contains `<LocationBreadcrumb cityName="Columbus" />`
    - Columbus.tsx contains `<NearbyAreas locationSlug="columbus" />`
    - NewAlbany.tsx contains `cityName="New Albany"` and `locationSlug="new-albany"`
    - UpperArlington.tsx contains `cityName="Upper Arlington"` and `locationSlug="upper-arlington"`
    - GroveCity.tsx contains `cityName="Grove City"` and `locationSlug="grove-city"`
    - No existing content was removed (spot check: Columbus.tsx still contains "German Village", Delaware.tsx still contains "Delaware County")
  </acceptance_criteria>
  <done>All 13 location pages render LocationBreadcrumb below the hero and NearbyAreas before the inline CTA, with correct cityName and locationSlug props. Full project type-checks cleanly.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with zero errors
2. `grep -c "LocationBreadcrumb" src/pages/locations/*.tsx` shows 13 files with count >= 2 (import + usage)
3. `grep -c "NearbyAreas" src/pages/locations/*.tsx` shows 13 files with count >= 2 (import + usage)
4. `npm run build` completes successfully
</verification>

<success_criteria>
- LocationBreadcrumb component renders "All Service Areas > {City}" with link to /locations on all 13 subpages
- NearbyAreas component renders 3-5 neighbor cards with links on all 13 subpages
- No existing page content modified
- Full TypeScript and build pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-internal-linking-navigation/02-01-SUMMARY.md`
</output>
